
# Generated by improve-harness.mjs from harness-issues.jsonl (which IS tracked — it is the source).
# Regenerated on every run, so its only diff is usually its own timestamp: run output, not state.
harness-loop/harness-improvements.md

# --- harness: run output. Regenerated every run; committing it is pure diff noise.
trace/
loop/current.json
loop/route-log.jsonl
