{
  "description": "GENERATED by tools/gen-agents.mjs from agents.manifest.json. Codex hooks are project-wide (agent TOML has no hooks field), so the guard identifies the running role from the HARNESS_AGENT environment variable that tools/codex-dispatch.mjs sets. Hooks require persisted trust: without it Codex SKIPS them silently and every write goes unguarded.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "node harness/tools/guard-write.mjs --runtime codex --from-env"
          }
        ]
      }
    ]
  }
}
