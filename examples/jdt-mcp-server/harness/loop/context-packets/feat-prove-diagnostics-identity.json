{
  "schema": "feature-context-packet/1",
  "feature": "feat-prove-diagnostics-identity",
  "mustRead": [
    "../src/lsp/diagnostics-cache.ts",
    "../src/tools/diagnostics.ts",
    "../test/integration/diagnostics.integration.spec.ts",
    "../test/lsp/diagnostics-cache.spec.ts",
    "tests/design/plans/TP-DIAG-0001/conditions/TCON-DIAG-0003.json",
    "docs/design/tool-surface.md"
  ],
  "mustNotRead": [
    "../src/workspace/workspace-pool.ts",
    "../test/workspace/workspace-succession.spec.ts",
    "docs/design/critique.md"
  ],
  "facts": [
    "The cache key is (workspaceId, canonicalized URI): PushDiagnosticsCache holds a Map<workspaceId, Map<uri, DiagnosticsReport>> and both absorb() and get() pass the URI through canonicalFileUri() (realpathSync + pathToFileURL; on error it returns the original string). This is MEASURED current-code state, not an assumption to recheck.",
    "TWO SURVIVING MUTANTS, created and measured by the checker on 2026-08-25 — do not rediagnose them; kill them: m1 = one flat Map keyed ONLY by URI shared across every workspace; m2 = when the queried workspace has no entry, get() scans every other workspace cache and returns the first entry found. Both stayed green 3/3 on test/integration/diagnostics.integration.spec.ts.",
    "THOSE TWO MUTANTS ARE NOT EQUIVALENT — measured by running test/lsp/diagnostics-cache.spec.ts on them: 3 red cases (m1) and 2 red cases (m2). Therefore the gap is on the java_diagnostics path, not in the cache unit oracle; do not rewrite existing unit cases or modify that unit file.",
    "THE MASKING MECHANISM is identified: TCON-DIAG-0003’s fixture supplies two DIFFERENT ABSOLUTE PATHS (<root>/workspace-a/... and <root>/workspace-b/...), so URI alone distinguishes them and workspaceId becomes a redundant key component. The only discriminating axis is two workspaceId values colliding on THE SAME canonicalized URI.",
    "THE REAL DEFECT IS MEASURED, not suspected: projectUris() in src/tools/diagnostics.ts unions facade.projectFiles() (caller spelling) with reader.list() (canonicalized) in a Set; two textually different strings pointing to one file are not deduplicated, so project scope returns two identical entries for one file. Checker reproduction: a file under os.tmpdir() on macOS, caller uses file:///var/..., cache stores file:///private/var/....",
    "No feat-diagnostics-cache case reaches the canonicalization branch: every URI in test/lsp/diagnostics-cache.spec.ts is a string that does not exist on disk (file:///projects/a/...), so realpathSync always throws and that branch has never executed successfully in a test.",
    "Both sides of the union in projectUris() have checker-approved reasons from feat-tool-diagnostics: the projectFiles() side keeps an unindexed file visible under not-reported rather than disappearing; the reader.list() side keeps a URI with real problems that the project file list does not name (generated code, files outside the source root) from being lost. The deduplication fix MUST NOT remove either side.",
    "Real JDT LS startup infrastructure already exists and runs: the locateJdtls/startWorkspace/openDocument/stop functions in test/integration/diagnostics.integration.spec.ts, plus the pinned JDT LS under .cache/jdtls-fixture. Copy that shape into a new file; ABSOLUTELY do not modify the old file — it belongs to blocked feat-prove-diagnostics and remains as a record.",
    "The baseline is green and stable: ./harness/init.sh is 124/124 and npm run test:integration is 191/191 as of 2026-08-25. The EMFILE directory-watching issue in attempts 2–3 was a host quota that has ended; it is not a code defect and needs no further investigation.",
    "Scope fence: the question ‘can two concurrently live workspaces ever hold the same canonical URI’ is an open design question, recorded in loop/goal.md under Human checkpoints. Do not answer it in this feature or expand the fixture to two JDT LS processes sharing one source file."
  ],
  "sourceInputs": [
    {
      "path": "../src/lsp/diagnostics-cache.ts",
      "sha256": "6a31e8ed1c3c551fb0799ad6a0fc49ae28ae9b6e6180bdc68db494802ccee88d"
    },
    {
      "path": "../src/tools/diagnostics.ts",
      "sha256": "05bfd21a214447a4141362f290085f5082a548a5c3b32bca9ad00e3191d51ef8"
    },
    {
      "path": "../test/integration/diagnostics.integration.spec.ts",
      "sha256": "92513f1bb56dfc12762a913c7ea9a9ff260e7eaf43e0329830067e3735b946d2"
    },
    {
      "path": "../test/lsp/diagnostics-cache.spec.ts",
      "sha256": "2d962d3ce4aa5cae5f866f26b179685b03eb41c2f13dab5754d490e64bb096f2"
    },
    {
      "path": "tests/design/plans/TP-DIAG-0001/conditions/TCON-DIAG-0003.json",
      "sha256": "b38a9320569189d6d482b4d4bd34f2b47564be8e36dc8c457ebc9864454dc42f"
    },
    {
      "path": "docs/design/tool-surface.md",
      "sha256": "ff1e19bce1db4590a326a1bc26ac279182acf1d8cc6a57f784c1be2494d04cc4"
    }
  ]
}
