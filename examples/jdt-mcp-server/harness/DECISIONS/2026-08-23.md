# Decision — period 2026-08-23

Split from `harness/DECISIONS.md` on 2026-08-25 according to Pattern B in
`harness/docs/reference/knowledge-layout.md`. The paths in the sections below are relative paths
adjective `harness/`.

---

## 2026-08-23 — `lsof` in TCON-SHIM-0002: DISCARD oracle fix, log environment conditions

- **Source:** FOLLOW-UP entry in verdict APPROVE of `feat-prove-daemon-lifecycle`.
  - Feature to keep `done`; evidence, checkerNotes and the oracle file were not affected by a single byte.
- **Decision:** DISCARD — do not cut new features, do not grant editing rights
  `test/integration/daemon-lifecycle.integration.spec.ts`, do not add conditional skips.
  - Retained section: an entry "System tools on which the integrator depends" in
    `harness/docs/testing-standards.md`, stating `lsof` + `procps`, how to identify symptoms, and
    installation command on Linux.
- **Cause 1 — failure mode is false red, and the baseline port does not reach it.** `npm test` and
  `npm run test:baseline` (the two commands that `init.mjs` runs) does not load this file; command
  `npm run test:integration` calls the new handle. Missing `lsof` does not cause the baseline to be red.
- **Cause 2 — the current error message self-reports the cause.** First printed break assertion
  verbatim `lsof returns []`, which means specifying the tool name. A skipped step conditionally changes the message
  That notice takes a bigger risk: the progress table measurement disappears silently on exactly the same machines
  least noticed.
- **Cause 3 — there is no portable alternative.** `/proc/net/unix` is only available on Linux and
  do not state owner pid if not scanning `/proc/*/fd`. `process.kill(pid, 0)` cannot be replaced
  `ps` for missing zombies, exactly where INV-SHIM-4 is concerned. Alpine also broke `ps` (busybox
  does not accept `-o pid= -p`), so "works on all minimalist images" is a completely different, larger scope
  much more than a single line of `lsof`.
- **Rejected option — cut a feature for this fix.** The only possible Falsifier is "oracle
  still works properly when lacking tools", an assertion about the test running infrastructure, not the product.
  Checker measured and recorded the results; repeating it as a feature adds no distinguishing power.
- **Constraints satisfied:** A-004 (v1 targeting macOS and Linux) is still respected — environmental conditions
  The field is now text, no longer the tacit knowledge of a checker.
- **Impact:** when a project adds a CI job on Linux, that job must install `lsof` and `procps` before the step
  `npm run test:integration`. This is the condition of future CI features, not today's scope.

---

## 2026-08-23 — Listener `error` left over from `probeDaemon`: don't cut features, fix wrong comments

- **Source:** FOLLOW-UP entry in verdict APPROVE of `feat-mcp-shim`.
  - That feature and `feat-daemon-supervisor` leave `done`, evidence and checkerNotes intact.
- **Decision:** DISCARD code correction — do not remove `error` listener in `probeDaemon`
  (`src/daemon/daemon-supervisor.ts`) and also not removed in `connectOnce` (`src/shim/mcp-shim.ts`).
  - Keep the correct part active: two limited edit permissions write to `context.note` of
    `feat-prove-daemon-lifecycle`, includes incorrect comments and a fixture element that kills mutants.
- **Cause 1 — no consumer touches Node's default error semantics.**
  - The only consumer of `handle.connection` is `attach()` in `src/shim/mcp-shim.ts`, and
    This function unconditionally attaches its own `on("error")` listener.
  - The listener exits immediately at `if (settled) return` and does NOT block propagation; Node still calls listener
    of shim, so the stderr stream is still written and `close` still triggers reconnect.
  - Race window does not exist: string `probeDaemon` resolve → `delegatedHandle` → `startDaemon`
    return → `establish` → `attach` includes only the synchronization code and microtask, and the socket `error` event
    is the I/O event in the following macrotask. No error event falls in the gap between two landmarks.
- **Cause 2 — the only possible falsifier conflicts with `INV-SHIM-3`.**
  - The only assertion that locks the fix at the process boundary is "launcher must die on socket
    first error". But `INV-SHIM-3` requires exactly the opposite: link errors should only be written to stderrReconnect transparently, the client does not have to be reinitialized.
  - Cutting a feature with such a falsifier is fabricating a requirement that is contrary to the approved invariant.
- **Cause 3 — the remaining assertion is just its own evidence.**
  - The only thing measured after the fix is `handle.connection.listenerCount("error") === 0`, an attribute
    white box calculation. No stdout lines changed (`INV-SHIM-1`), no pids changed (`INV-SHIM-2`),
    no entries in the change progress table (`INV-SHIM-4`).
  - This is the exact counterexample to "build with no independent proof" that the previous two cuts rejected. Connectivity
    The ability to enter `feat-prove-daemon-lifecycle` just to satisfy `build-unproven` is forced: that oracle is not
    judgment is affirmed above.
- **The DO NOT discard part — the comment in `src/shim/mcp-shim.ts` gives the wrong reason.**
  - The current sentence says "A socket with no `error` listener throws". Because the extra listener is always present
    socket delegated, if the `on("error")` line is missing, the error disappears without a trace and is not thrown.
  - Checker has been measured: mutant CE1del completely deletes the 12th shift timer handler at stderr assertion, no crash.
  - The DISCARD decision above makes this annotation MORE IMPORTANT: the extra listener stays permanently,
    So the comment must state the correct reason, otherwise the next editor will delete that line and the error will be lost in silence.
- **NOT discard part — `parsed !== null` clause in `isMcpMessage` (checker item 7a).**
  - The `noise` array of case 3 is missing the string `"null"`, so the mutant omitted this clause to live 13/13. With that mutant,
    `isMcpMessage("null")` returns `true` and the `null` stream flows directly to the client's stdout — violation
    `INV-SHIM-1` indeed.
  - Adding exactly one element is enough; The two assertions count in a self-scaling shift according to `noise.length`.
- **Reject checker item 7b — `link.writable` clause in `sendToDaemon`.**
  - The checker himself puts the damage into the admitted `INV-SHIM-3` section (call was flying when the daemon died),
    and the `link !== undefined` window but the `writable` window is so narrow that no one can reach it.
- **Dual bearer fix — `feat-prove-daemon-lifecycle`, not a new feature.**
  - `feat-mcp-shim` already has evidence so cannot return to the oracle class; No other features included
    The plan also owns `src/shim/mcp-shim.ts` or `test/shim/mcp-shim.spec.ts`.
  - Edit permission in src is locked as COMMENTARY, allowing the checker to verify the diff in a few seconds.
- **Fix — unblock `feat-prove-daemon-lifecycle`** (`blocked` → `not-started`, attempts still 0/3,
  The old blocking note remains the same and has an additional paragraph stating the reason for removal).
  - The exit condition stated by the blocking note itself is met: two dependencies with `done`, `handle.daemonPid`
    overlays daemon-pid response, `handle.shutdown()` overlays shutdown contract, fixture spawns real workspace
    already exists from `feat-workspace-pool`.
  - Only the CLI entry point is missing (`package.json` does not declare `bin`), and three falsifiers of oracle
    This doesn't need it: ca process-boundary of `feat-mcp-shim` already demonstrates a child `node` process
    Loading `startShim` directly is enough. Oracle really needs the CLI to be `feat-prove-cross-process-integration`,
    still `blocked`.
  - If left `blocked`, the test-implementer rule permanently blocks it and `INV-SHIM-2` and `INV-SHIM-4`
    There are no oracles left.
- **Fix — restore `readyForCheck: false` for `feat-mcp-shim`.**
  - This field is lost again when the entry is recorded in the approval round, like the previous two rounds. No change
    state or evidence.
- **Impact:** number of features unchanged — 34 (19 builds, 14 proofs, 1 baseline), DAG 15 levels deep.
  - Fixed: `status`, `checkerNotes`, `context.touches` and `context.note` of
    `feat-prove-daemon-lifecycle`; `readyForCheck` of `feat-mcp-shim`.
  - `loop/route-log.jsonl` records request `follow-up:feat-mcp-shim:<hash>`.
  - `check-plan.mjs` also corrects a finding with an exception being accepted (`context-touches` on
    `feat-workspace-pool`).

---

## 2026-08-23 — Unsubscribe function discarded by `DiagnosticsCache.attach()`: no feature cut, attach lifecycle binding

- **Source:** FOLLOW-UP entry in verdict APPROVE of `feat-lsp-notifications`.
  - That feature keeps `done`, attempts 2/3, evidence and checkerNotes intact.- **Decision:** DISCARD — do not cut the `LspNotificationSource.onNotification()` port expansion feature.
  - Instead: an unconditional binding in `context.note` of `feat-tool-layer-core`
    (`not-started`, attempts 0), which owns `cache.attach(workspaceId, lease.client)`.
  - Constraint: attach EXACTLY ONCE for each `LspClient` at the pool spawn workspace.
  - Bind: detach in the right place to call `cache.forget(workspaceId)` at evict, do not attach according to lease.
- **Cause 1 — leaks do not accumulate over the pool lifecycle.**
  - `LspClient` only spawns at `src/workspace/workspace-pool.ts:323`, one instance per JDT LS process
    statue.
  - `#evict()` removes entries from `#entries` then stops the process; `acquire()` then falls into the branch
    `entry === undefined` and a new spawn, i.e. a NEW `LspClient`.
  - Consequence: the evict-then-reactquire cycle never reuses the old client; client and
    Its `#notificationHandlers` is trashed at once.
- **Cause 2 — realized leak size is 0.**
  - `grep "attach("` on `src/` only returns the port declaration line (`diagnostics-cache.ts:94`) and the
    deploy (line 142). No production calls exist.
- **Cause 3 — lifecycle constraints close risk more thoroughly than port fixes.**
  - The real risk is attaching by lease: one warm process serves many leases, so the handler array is long
    added after each tool call and the dispatch cost per frame increases linearly.
  - Port fix does NOT block that scenario if detach is not called; one-attach-one-client binding
    blocked even if the port remains the same.
- **Rejected option — cut a small `build` feature that opens the port and forwards the remove function.**
  - Confirmation that "remove function actually unsubscribes from the source" is not observed at the acceptance boundary:
    No response tools changed, no `INV-DIAG-*` violated.
  - Its only proof is its own unit test — the correct counterexample "build with no
    independent proof"; appending to `feat-prove-diagnostics` just to satisfy `build-unproven` is forced.
  - It forces editing `test/lsp/diagnostics-cache.spec.ts` (`RecordingSource` must return the remove function) — property
    oracle has approved `feat-diagnostics-cache`, which means limited edit permissions must be granted.
  - When the actual connection needs to be re-attached on the same client, the edit is two lines in
    `src/lsp/diagnostics-cache.ts` and at that time someone actually called to prove it.
- **Checked and ruled out — `LspNotificationSink` does not have similar errors.**
  - Port at `src/workspace/file-sync-watcher.ts:54` has only `notify(method, params)`: SEND port,
    There's no registration so there's nothing to remove. The `fs.watch` lifecycle is managed by the watcher's `close()`.
- **Uncommented `feat-diagnostics-cache`:** that feature is `done`.
  - It would be more effective to put a note on the `not-started` feature that actually operates the attach lifecycle.
- **Fix — restore `readyForCheck: false` for `feat-lsp-notifications`.**
  - This field is missing when the entry is recorded in the approval round, causing `check-plan.mjs` to report
    `required-field` and `ready-invalid`.
  - All other `done` features have this value; `route.mjs` does not read this field anywhere
    Which branch? Do not change status or evidence.
- **Edit — rotate period 2026-08-21 to `harness/DECISIONS/2026-08-21.md`.**
  - This file hits 298 lines after adding the above entry, close to the 300 budget of `docs/constraints.md`.
- **Impact:** number of features unchanged — 34 (19 builds, 14 proofs, 1 baseline), DAG 15 levels deep.
  - Fixed: `context.note` of `feat-tool-layer-core`, `readyForCheck` of `feat-lsp-notifications`.
  - `loop/route-log.jsonl` records request `follow-up:feat-lsp-notifications:a09713071c27`.
  - `check-plan.mjs` also corrects a finding with an exception being accepted (`context-touches` on
    `feat-workspace-pool`).

---

## 2026-08-23 — `LspClient` notification routing cut to `feat-lsp-notifications`

Source: FOLLOW-UP entry in verdict APPROVE of `feat-diagnostics-cache`. That feature remains the same
`done`, attempts 1/3, evidence and checkerNotes are untouched.

- **Decision:** cut a new `build` feature, `feat-lsp-notifications` ("lsp-client: notificationtransport in both directions"), depends on `feat-lsp-client`, `touches` is `src/lsp/lsp-client.ts`
  same new spec file. It adds `notify(method, params)` to the send direction and
  `onNotification(method, handler)` for the receiving dimension. Includes context package
  `loop/context-packets/feat-lsp-notifications.json`.
- **Cause — the void is real and has a dependent invariant:** `src/lsp/lsp-client.ts` is not present
  Which public method sends or receives notifications; in `#handleMessage`, line
  `if (typeof message.id !== "number") return;` silence all messages that do not carry an id. The consequences are all
  the two declared ports have no connectors: `LspNotificationSource` of `diagnostics-cache` is nil
  ever receive `textDocument/publishDiagnostics` (`INV-DIAG-1/2/3` is not proven at the cumulative level
  case), and `LspNotificationSink` of `file-sync-watcher` has no real `notify()` to call
  (`INV-SYNC-1/2/3` same situation). `feat-prove-diagnostics` is `blocked` with the reason being
  need "real publishDiagnostics delivery".
- **Cause — combining two dimensions into one feature:** both dimensions are the same concept (message
  JSON-RPC does not carry `id`), the same file, and each dimension alone still leaves a hanging port. Split
  double is two dispatches that pay for a single seam, exactly what `cutting-rules.md` calls splitting
  minor an inseparable act.
- **Do not cut new `prove` features:** `feat-prove-sync` and `feat-prove-diagnostics` (both `blocked`,
  attempts 0) now depends on `feat-lsp-notifications`. Those two oracles must run through JDT LS
  true, so they correctly judge this claim; `cutting-rules.md` allows an overlay `prove` feature
  multiple `build` features when an acceptance script actually evaluates them at the same time. Thanks to that regulation
  The `build-unproven` rule is met without adding duplicate oracles.
- **Rejected option — DISCARD, let `feat-tool-layer-core`/`feat-mcp-shim` swallow itself.** Two features
  That feature has the WIRING part (`cache.attach(workspaceId, lease.client)`, the watcher sink points to
  `lease.client`), but cannot wire into a capability that does not yet exist. A line with gender has been written
  Limit the `context.note` of `feat-tool-layer-core` to lock in that boundary.
- **Rejected alternative — add reverse dependency edge from `feat-file-sync-watcher` and
  `feat-diagnostics-cache` to new features.** These two `done` features are approved by checker
  correct because they declare a narrow port instead of modifying `lsp-client.ts`; Their behavior is fully proven
  for port, not for LspClient. Adding an edge turns two completed features into dependencies
  on work that has not yet begun without adding a single verifiable assertion.
- **Data integrity fix:** restore `readyForCheck: false` for `feat-file-sync-watcher`
  and `feat-diagnostics-cache` — this field is dropped when maker/checker writes the entry, causing
  `check-plan.mjs` reports `required-field`. Do not change status or evidence.
- **Impact:** 33 → 34 features (19 builds, 14 prove; DAG 15 levels deep at
  `feat-prove-code-actions`); `dependencies` and `context.note` of `feat-prove-sync` and
  `feat-prove-diagnostics`; `context.note` of `feat-tool-layer-core`; new context pack;
  `loop/route-log.jsonl` logs request `follow-up:feat-diagnostics-cache:11f803c5d8cd`. `check-plan.mjs` also exists
  Exactly one finding had an exception accepted (`context-touches` on `feat-workspace-pool`).