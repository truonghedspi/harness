# Decision archive — table of contents

Closed periods of `harness/DECISIONS.md`, split according to Pattern B in
`harness/docs/reference/knowledge-layout.md`. Read the table of contents first, then open the correct file; don't read
end-to-end archive.

| Ky | Decisions in file | Open when |
|---|---|---|
| [2026-08-19](2026-08-19.md) | Select Node/TypeScript daemon + shim stdio via Unix socket, reject Option B (Java/LSP4J) and Option C (Streamable HTTP only); Gradle postponed, v1 only supports Maven | Some people suggest re-engineering the transport axis, changing the deployment language, or asking why the workspace identity is tied to `pom.xml` |
| [2026-08-20](2026-08-20.md) | First feature cut (32 features, 10 components); build order encoded into real DAG edge; falsifier of `feat-lsp-client` extracts `INV-POOL-3` instead of generating `INV-LSP-*`; Defer Streamable HTTP (X-010), GC `-data` directory (X-006) and retry after crash (X-009) | Someone asked why a component is cut into a build/prove pair, why the tool feature depends on the prove feature of the previous stage, or why the HTTP front door and GC don't have any features yet |
| [2026-08-21](2026-08-21.md) | Five decisions of the same type: loosen existing `feat-prove-routing` and `feat-jdtls-provisioner` (add `TCON-ROUTE-0005/0006/0007`, nested reactor branch, module path, provisioner success path) instead of cutting new feature | Someone is planning to cut a separate feature for a shared space seam, oracle and invariant files with an existing `prove` feature |
| [2026-08-22](2026-08-22.md) | Timeout missing in two built-in specs (measured that timeout is disabled with sync shifts) — just a note; three backlogs from `feat-workspace-pool` handled at three levels: limited edit permissions, new prove feature `feat-prove-workspace-identity`, and unblocking `feat-prove-pool-crash-handling` | Someone asked why a synchronous `node --test` case is not timeout protected, why identity is cut into its own feature, or why a `blocked` feature is returned to `not-started` |
| [2026-08-23](2026-08-23.md) | Four FOLLOW-UP format decisions: DISCARD the oracle edit section `lsof` of TCON-SHIM-0002 and write the environment conditions to the docs; listener `error` left over from `probeDaemon` — fix incorrect comments instead of cutting functionality; deregister function discarded by `DiagnosticsCache.attach()` — bind the attach lifecycle instead of cutting the feature; `LspClient`'s notification route cuts to `feat-lsp-notifications` | Someone asked why a checker's FOLLOW-UP ends with DISCARD or a comment, or why wiring notifications into a separate feature instead of being swallowed by `feat-tool-layer-core` |