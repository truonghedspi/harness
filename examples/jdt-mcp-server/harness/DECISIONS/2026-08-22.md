# Decision — period 2026-08-22

Session closed, separated from `harness/DECISIONS.md` on 2026-08-23 according to Pattern B in
`harness/docs/reference/knowledge-layout.md`. The content remains the same, no wording is changed.

---

## 2026-08-22 — Timeout missing in two built-in specs: recorded, no features cut

Source: FOLLOW-UP entry in verdict APPROVE of `feat-prove-workspace-identity`. That feature holds
original `done`, attempts 1/2, evidence untouched.

- **Decision:** do not cut new feature for adding `{ timeout: N }`
  `test/integration/workspace-identity.integration.spec.ts` and
  `test/integration/project-router.integration.spec.ts`. Retain the effective part with a tonic line
  added to `context.note` of `feat-prove-provisioner` (currently `blocked`, will return to oracle class):
  Any asynchronous test cases written by that turn must have a limited timeout.
- **Cause — the proposed measure is invalid in one of the two files:** all seven cases of
  `project-router.integration.spec.ts` is the synchronous callback. Measured on `node --test`: one sync shift
  cycle 4,000 ms with `{ timeout: 500 }` still says `ok` after 4,000 ms, because timer cannot run when
  event loop is blocked; The same option punctually cancels an asynchronous shift. Add timeout to sync file
  only creates a false sense of security.
- **Cause — the remainder is too small compared to the cost of one feature:** three internal asynchronous shifts
  `workspace-identity.integration.spec.ts` is indeed protected, but `check-plan.mjs` is required every
  The `build` feature has a `prove` feature that judges it (`build-unproven`), so the minimum price is
  two dispatches for three optional parameters — disproportionate to the low risk level of the checker itself
  logged (fixture uses only the filesystem and an injected spawn hook).
- **Rejected alternative:** a static scan script `test/integration/**` appended to the `check` script of
  `package.json` — it will force the entire file to synchronize with invalid timeout, meaning the above misunderstanding is changed to
  rules. *Reject:* edit `dependencies` of a `prove` feature that has been `done` to borrow judgment.
- **Remaining risk, has become a human checkpoint:** `harness/init.mjs` calls `spawnSync` no
  `timeout`, so a hanging sync will hang indefinitely for `./harness/init.sh`. This is the harness class,
  not project scope — recorded in `loop/goal.md`.
- **Effect:** number of features remains 33; `context.note` of `feat-prove-provisioner`; a human
  new checkpoint in `loop/goal.md`; `loop/route-log.jsonl` records requests
  `follow-up:feat-prove-workspace-identity:b12374c76963`.

---

## 2026-08-22 — Three backlogs from `feat-workspace-pool`, three intervention levels

Source: three FOLLOW-UP entries in verdict APPROVE of `feat-workspace-pool`; That feature remains the same
`done`, attempts 1/3, evidence. Detailed operations and accepted exceptions of `check-plan.mjs`
located in `session-handoff.md` on the same date.

- **Oracle error `TCON-POOL-0003` — note with limited edit permission, no scope created.**
  Allows just one modification: shrink the absence assertion from `pool.status()` to the workspace set
  has evicted and has not reacquired; asserts the existence of the `-data` directory, i.e. the falsifier of
  `INV-POOL-4`, locked and not allowed to touch. There is no need for a test-designer turn, because the text has been evaluated by the
  the condition which only binds the absent state at the time of evict — this is the restoration of neutrality
  oracle's reality, not new conditions. *Deny:* let the router take this to the oracle layer; rules
  test-implementer only matches when `evidence` is empty, which feature brought evidence RED on 2026-08-20,
  and deleting evidence to force the router is falsifying history. *Affects:* `context.*` and `checkerNotes` of
  `feat-prove-pool-lifecycle`; new context pack `loop/context-packets/feat-prove-pool-lifecycle.json`.

- **Identity `project-router` ↔ `workspace-pool` — new prove feature `feat-prove-workspace-identity`.**
  Identity calculated twice independently (`project-router.ts:47`, `workspace-pool.ts:171`), no test compares
  Compare two results: claim spans the boundary of two components. Falsifier extracts `INV-POOL-1` +
  `INV-ROUTE-3`; `INV-POOL-1` has no previous conditions covered, so the test-designer rule matches and
  The feature goes through the oracle layer before reaching the maker. *Decline:* expand falsifier of`feat-prove-pool-lifecycle` — that oracle uses a fake spawner, does not call project-router, and because it already has
  evidence, so claiming to add more will go straight to the maker without the oracle. *Rejected:* add build
  extract shared identity function — combining two hash points is the implementation choice, the final plan
  observable behavior. *Effects:* 32 → 33 features; `loop/goal.md` updates the number.

- **Soft stop SIGTERM before SIGKILL — note only.** None of the invariants state stop order;
  `INV-POOL-3` only requires that the in-flight request ends with an error in the deadline, and the mutant deletes the call
  `child.kill` with SIGTERM doesn't violate that. Closing this behavior requires a new invariant, the question
  design. *Affects:* `context.note` of `feat-prove-pool-crash-handling`; human checkpoint.

- **Unblock `feat-prove-pool-crash-handling`** (`blocked` → `not-started`, attempts still 0/3, note
  old block remains the same). The block note itself states the exit condition — two dependencies with `done` — and now done
  enough. Blocking because the dependency is not ready is different from blocking because of running out of attempts; Only the latter type requires a decision maker
  determined. If left `blocked`, the test-implementer rule permanently removes it and `INV-POOL-3` has no oracle.