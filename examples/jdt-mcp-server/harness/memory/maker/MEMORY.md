# Maker memory — JDT MCP Server

Index of what the maker agent has learned across runs (`harness/docs/reference/agent-memory.md` documents the schema and why). One line per entry, always loaded — keep it
short; the reasoning lives in the linked file, read that only when the line looks relevant.

Write a new entry when a checker rejection, a failed reproduction, or a recurring review comment
teaches something the *next* maker run on this project shouldn't have to rediscover. Don't write
one for routine, expected outcomes — that's noise, not a lesson.

Write entries and hooks **in English**, whatever language the rest of the project uses: memory is
addressed to whichever agent reads it next, not to this project's readers. Entries already written
in another language stay as they are — this governs what gets written from now on.

<!-- - [Title](slug.md) — one-line hook -->
- [Đếm tiến trình con phải dùng một file log cho mỗi child](counting-spawns-needs-one-log-file-per-child.md) — fixture ghi chung một file bằng `>>` bị xen kẽ ở mức syscall, làm oracle đếm thiếu tiến trình và đổ lỗi nhầm cho mã nguồn; ghi ra `"$LOGDIR"/"$$".txt` và chờ một khoảng lắng trước khi khẳng định "đúng một tiến trình".
- [Sự kiện `fs.watch` không phải bằng chứng rằng tệp đã đổi](fs-watch-events-are-not-evidence-of-a-diff.md) — macOS phát lại sự kiện của những lần ghi ngay trước khi `watch()` được cài, nên "đường dẫn được sự kiện nêu tên và có mặt ở cả hai phía ⇒ Changed" sinh thông báo giả (một lần re-import `pom.xml` thừa mỗi lần khởi động); dùng sự kiện chỉ để lên lịch flush, và quyết định loại thay đổi bằng diff `(mtimeMs, size, ino)` với ảnh chụp lần settle trước.
- [Cửa sổ khởi động FSEvents nuốt lần ghi đầu tiên](fsevents-startup-window-swallows-the-first-write.md) — trên macOS, libuv khởi động luồng FSEvents sau khi `fs.watch()` trả về, nên lần ghi rơi vào cửa sổ đó không bao giờ tới; biểu hiện là im lặng rồi timeout ở lần chờ đầu tiên của **ca nào ghi trước tiên** (đo được 2/4 lần chạy song song đỏ, chạy riêng thì 12/12 xanh); sửa bằng tệp mồi mà watcher bỏ qua theo cấu trúc, chỉ hích khi `lastChangeAt` còn `undefined`.
- [Hai lớp canh gác cho cùng một thời hạn làm mutant sống sót](two-guards-for-one-deadline-hide-a-live-mutant.md) — mutant xoá đúng falsifier của `INV-READY-3` vẫn 6/6 xanh vì thời hạn được siết ở hai chỗ (vòng `settleBy` và `timeoutMs` kẹp trong `#probeOnce`); mutant sống không có nghĩa là tương đương — tìm đầu vào vô hiệu hoá lớp canh gác còn lại, ở đây là probe tiêm được cố tình bỏ qua `timeoutMs`, và chấp nhận `cancelled` do treo là bằng chứng đỏ hợp lệ cho bất biến "phải settle trong hạn".
- [Process-boundary Level 3 oracle for a build feature](process-boundary-oracle-for-build-features.md) — checker rejection for an in-process mock on a real spawn/exit boundary is the maker's own oracle to write (build-kind), not test-designer's; materialize scripted child fixtures to a tmpdir at test run time, and red-prove the oracle by temporarily breaking the exact source behavior before trusting green.
- [Trên Unix socket, "N tiến trình cùng bind" không tự báo EADDRINUSE](unix-socket-bind-race-needs-an-o-excl-lock.md) — probe `connect()` trước khi bind là chưa đủ: mỗi launcher đến sau `unlink` tệp socket của kẻ trước rồi bind tệp mới, nên 5 lần khởi chạy đồng thời cho ra 5 daemon mà không lần nào báo `EADDRINUSE` (đo bằng mutant M4); phải bọc `probe → unlink stale → listen` trong một lock `openSync(path,"wx")` giữ suốt vòng đời daemon và mang pid.
- [Cô lập từng trường của bộ ba `(mtimeMs, size, ino)`](isolating-one-field-of-a-stat-stamp.md) — ca temp-write-then-rename đổi inode ở mọi trường hợp nên không bao giờ ghim được vế mtime hay vế size; `writeFileSync` lên đường dẫn đã tồn tại giữ nguyên inode (`O_TRUNC`, không `unlink`), nên ghi tại chỗ cùng số byte cô lập mtime còn ghi tại chỗ khác số byte kèm `utimesSync` trả mốc cũ cô lập size — với điều kiện mốc nền được đóng băng vào quá khứ **trước** `start()` và cặp ghi/`utimesSync` nằm trong cùng một khối đồng bộ.
- [Lớp trong "không quan sát được" thường lộ ra ở cổng tiêm được](inner-layer-shows-up-at-the-injectable-seam.md) — checker bỏ qua mutant RM8 vì cho rằng lớp kẹp timeout bên trong không đo được, nhưng đại lượng bị làm sai lại được **trao ra** qua tham số `ProbeOptions.timeoutMs` của cổng `probe` công khai: tiêm probe, ghi lại ngân sách nhận được, đòi nó nằm trong `[1, hạn của caller]` là giết ngay; kèm mẹo thay `await` không giới hạn bằng `Promise.race` với watchdog để mutant chết bằng một dòng đỏ có tên thay vì treo và huỷ mọi ca sau.
- [Cấp ngân sách ở lớp test mới chặn được nửa cơn treo](bounding-a-hang-at-the-test-layer-is-not-enough.md) — bọc `shutdown()` trong `Promise.race` làm mọi ca đỏ có tên nhưng tiến trình vẫn không thoát (đo: SIGKILL ở 120 s), vì thứ giữ event loop là một `net.Server` còn listening chứ không phải một promise, nên hạn chót phải nằm trong `src`; kèm ba hệ quả: đường cưỡng bức sẽ biến mutant thành tương đương nếu ca không đòi một ngân sách nằm hẳn dưới nó, `# cancelled != 0` với `# fail == 0` nghĩa là có vòng chờ dựng trên timer đã `unref()` làm cạn event loop, và `listen()` trên đường dẫn vượt `sun_path` **thành công** rồi cắt cụt tên chứ không hề báo lỗi.
- [Giá trị trả về mà không ca nào gọi là mutant no-op đang chờ sống sót](returned-closures-need-a-case-that-calls-them.md) — hàm gỡ đăng ký do `onNotification()` trả về bị mutant `return () => {};` làm rỗng mà 9/9 ca vẫn xanh, vì bằng chứng duy nhất cho nó là `tsc` nói kiểu khớp; kèm hai mẹo: ca phủ định phải có mỏ neo dương trước đó (`calls === 1` khi đăng ký còn hiệu lực) nếu không nó xanh một cách rỗng, và handler tự gỡ mình giữa lúc dispatch làm `splice` dịch chỉ số nên sibling bị bỏ qua im lặng.
- [Bất biến về stdout chỉ đo được ở tiến trình thật](stdout-invariant-needs-the-real-process.md) — oracle tiêm một `Writable` vào tuỳ chọn `stdout` KHÔNG bao giờ thấy `console.log`, vì hàm đó ghi vào `process.stdout` của tiến trình; đo được bằng mutant chèn đúng một dòng log: 6 ca trong tiến trình xanh, chỉ ca chạy module như tiến trình con thật và đọc byte trên ống stdout thật báo đỏ — mọi bất biến "không có gì ngoài X ra stdout" phải có ít nhất một ca ở biên tiến trình, kèm một ca cho nhánh khởi động thất bại đòi `stdout === ""`.
- [Chặn nhịp một cổng tiêm được, thay vì thay nó bằng đồ giả](gating-a-real-seam-instead-of-faking-it.md) — ca "giết thành phần A, để B thế chỗ trên cùng một socket" tự tạo chạy đua: ngay khi A chết đường dẫn trống và chính thành phần đang kiểm thử sẽ tự bind nó, nên ca xanh vì lý do khác nhau mỗi lần chạy; sửa bằng cách cho cổng `launch` vẫn gọi hàm thật nhưng `await` một promise do test nắm, và luôn chờ trạng thái quan sát được của thành phần (`stats().connected` chuyển false) chứ không chờ con mồi chết — kèm mẹo gắn `pid` vào câu trả lời để chứng minh một tiến trình thật sự khác đã trả lời.
- [Recorder trên `process.stdout.write` bắt luôn kênh báo cáo của `node --test`](stdout-recorder-must-ignore-the-runner-channel.md) — `node --test` chạy mỗi tệp spec trong tiến trình con và báo cáo qua chính `process.stdout` bằng bộ tuần tự V8, nên recorder ngây thơ đỏ giả với khung `test:start` nhị phân; đo được rằng `console.log` và mọi dòng log của mã nguồn luôn tới dưới dạng `string` còn kênh runner luôn là `Buffer`, nên chỉ ghi chunk chuỗi, kèm khẳng định `NODE_TEST_CONTEXT === "child-v8"` và một mỏ neo dương để `recorded() === ""` không xanh rỗng.
- [Ghim "peer chết giữa một thông điệp" bằng một lần ghi socket duy nhất](pinning-a-mid-message-death-deterministically.md) — kịch bản này chỉ đo được nếu mảnh cụt chắc chắn đã nằm trong bộ đệm khi peer bị SIGKILL, và một `setTimeout` không chứng minh được điều đó; gửi mảnh cụt trong **cùng lần `socket.write()`** với câu trả lời trước đó biến "câu trả lời đã lên stdout" thành bằng chứng cơ học — nhờ đó mutant kép (bỏ `flush` + dùng chung framer) mới lộ ra là làm biến mất hẳn một response.
- [Điểm cắt giữa ký tự UTF-8 phải chứng minh bằng byte](proving-a-chunk-boundary-inside-a-utf8-character.md) — fixture *chứa* ký tự đa byte không phân biệt được `StringDecoder` với `chunk.toString()` (mutant `D1` sống hai lượt vì mọi ranh giới rơi đúng đầu ký tự); khẳng định `(buffer[cut] & 0xc0) === 0x80`, chứng minh chunk tới thành hai lần đọc bằng một thông điệp neo đi chung chunk cộng khoảng lắng, so sánh toàn văn chứ không phải "parse được thành JSON" — và cho framer của chính oracle dùng `StringDecoder`.
- [Sinh một sự kiện `error` thật trên socket](triggering-and-observing-a-real-socket-error.md) — phá socket phía peer **rồi** ghi ngay một message lớn cho `EPIPE` 5/5 lần, thứ tự ngược lại với message nhỏ cho 0/5, `resetAndDestroy()` ném `ERR_INVALID_HANDLE_TYPE` trên Unix socket; và xoá hẳn listener `error` **không** làm chết tiến trình vì `probeDaemon` để sót một `once("error")` đã settled nuốt lỗi đầu tiên, nên ca phải có mỏ neo dương trên stderr.
- [`LspClient` chưa định tuyến notification](lsp-client-khong-dinh-tuyen-notification.md) — `onRequest` chỉ chạy khi thông điệp mang `id`, nên notification LSP (publishDiagnostics, language/status) bị bỏ rơi im lặng và không có `notify()` cho chiều gửi; component tiêu thụ đẩy phải khai báo cổng hẹp trong tệp của chính nó (`LspNotificationSource` / `LspNotificationSink`) — sửa `src/lsp/lsp-client.ts` là scope bleed theo phán quyết ở `feat-file-sync-watcher` — và phải ghi trong `checkerNotes` rằng phần dispatch thật còn thiếu.
- [`npm test -- <file>` nối thêm vào glob, không thu hẹp nó](npm-test-with-a-file-does-not-narrow-the-suite.md) — script `test` đã chứa ba glob nên lệnh trong `verification` chạy TOÀN BỘ suite; đo được ở `feat-tool-references`: 9 fail/100 trong khi oracle của chính tính năng xanh 12/12, toàn bộ lỗi thuộc hai tệp của maker chạy song song — đo bằng `node --test <một tệp>`, rồi quy trách nhiệm theo tệp bằng vòng lặp trước khi kết luận.
- [Máy này không có `timeout`, và một lượt mutant thiếu nó im lặng hoàn toàn](no-coreutils-timeout-makes-a-mutant-pass-print-nothing.md) — cả `timeout` lẫn `gtimeout` đều vắng mặt trên macOS, `command not found` bị chính `grep` của script nuốt, nên bốn mutant in ra bốn tiêu đề và không dòng kết quả nào — trạng thái không phân biệt được với "mọi mutant đều sống sót"; dùng `--test-timeout` của node, và luôn giữ `# tests`/`# fail` trong mẫu grep làm bằng chứng rằng lệnh có chạy.
- [`timeout` không có trên macOS: lượt dựng mutant im lặng](gnu-timeout-missing-makes-a-mutant-pass-silently-empty.md) — `timeout 120 node --test ... | grep "^not ok"` in ra đúng năm tiêu đề mutant và không một dòng đỏ nào, trông hệt như "mọi mutant đều sống sót", vì darwin không có `timeout` của GNU nên `node` chưa từng chạy; dùng `node --test --test-timeout=30000` (ngân sách của chính runner, ca bị cắt vẫn in `not ok` có tên) và bắt script kêu lên khi một lần chạy không sinh ra dòng `# tests N`.
- [Một handle bị rò biến mutant đỏ thành một lần treo](a-leaked-handle-turns-a-red-mutant-into-a-hang.md) — mutant xoá đường dọn dẹp cũng xoá thứ giữ cho tiến trình test thoát được, nên `node --test` bị `SIGKILL` sau timeout và không in `not ok` nào (đo: M6 mất 600 s, không dòng đỏ); ca kiểm chứng một bất biến về dọn dẹp phải có lưới an toàn **không đi qua đường đang bị kiểm chứng** (`t.after` đóng thẳng từng watcher, chứ không chỉ `pool.close()`), phải đăng ký cleanup ngay khi tài nguyên ra đời chứ không gọi `close()` giữa thân ca, và script dựng mutant phải có `timeout` + `killSignal` riêng cho mỗi lần chạy.
- [Mọi ca đều `await` xong tài nguyên, nên trạng thái nửa-dựng không ca nào bước vào](every-case-awaits-the-resource-so-half-built-never-runs.md) — sáu mutant tự dựng đều chết đúng dự đoán mà vẫn lọt một lỗi rò handle thật, vì mọi ca mở đầu bằng `await pool.acquire()` nên `"starting"` chỉ được gán chứ không ca nào quan sát; đếm số ca **đọc** mỗi giá trị của kiểu trạng thái để tìm vùng mù, và dựng cửa sổ hai pha bằng hai `deferred` quanh seam (`spawnReached` + `spawnGate`) chứ đừng dùng `setTimeout`; kèm hai mẹo: `grep` cả template string khi bất biến nói "chuyển đổi tại đúng một chỗ", và emoji KHÔNG kiểm chứng được cặp surrogate (cần chữ cái astral `𝐀` nằm trong một định danh).
- [`EMFILE` từ `fs.watch()` là hạn ngạch của host, và nó tự lành](emfile-from-fs-watch-is-a-host-quota-that-heals.md) — baseline đỏ trong `DiskFileSyncWatcher` trông hệt như rò file descriptor, nhưng một tiến trình Node độc lập không import module nào của dự án cũng `EMFILE` trên thư mục temp TRỐNG trong khi theo dõi một tệp và mở 300 fd `/dev/null` vẫn xanh — ba vế đó cùng bác bỏ cả lỗi watcher lẫn cạn fd thường, thủ phạm là hạn ngạch theo dõi thư mục của macOS bị nhiều tiến trình test song song làm cạn; nó tự phục hồi (đo lại một ngày sau: 202 lần watch xanh, baseline 124/124, không sửa dòng nào), nên hãy giữ `readyForCheck: false` và đo lại lúc máy rảnh thay vì tiêu một lượt thử lại để vá.
- [Ca `feat-001` đỏ có thể là suite unit của maker khác](integration-gate-tests-mirror-other-makers-red.md) — các ca baseline-gate gọi `init.mjs` trong tiến trình con, mà cổng đó chạy `npm test` toàn bộ, nên chúng phản chiếu tệp đang sửa dở của maker song song (đo: 5 đỏ + `# tests 320`, chạy lại `# tests 181`, 0 đỏ, không sửa gì); đọc TÊN ca đỏ trước khi đọc số, `# tests` nhảy giữa hai lần chạy là dấu hiệu cây nguồn đang bị sửa đồng thời, và `npm test -- <file>` KHÔNG thu hẹp phạm vi mà chỉ nối thêm vào glob sẵn có.
