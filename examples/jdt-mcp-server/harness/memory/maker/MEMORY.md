# Maker memory — JDT MCP Server

Index of what the maker agent has learned across runs (`harness/docs/reference/agent-memory.md` documents the schema and why). One line per entry, always loaded — keep it
short; the reasoning lives in the linked file, read that only when the line relevant.

Write a new entry when a checker rejection, a failed reproduction, or a recurring review comment
teaches something the *next* maker run on this project shouldn't have to rediscover. Don't write
one for routine, expected outcomes — that's noise, not a lesson.

Write entries and hooks **in English**, whatever language the rest of the project uses: memory is
addressed to whichever agent reads it next, not to this project's readers. Entries already written
in another language stay as they are — this governs what gets written from now on.

<!-- - [Title](slug.md) — one-line hook -->
- [Counting child processes must use a log file for each child](counting-spawns-needs-one-log-file-per-child.md) — fixture writes to the same file with `>>` interleaved at syscall level, causing oracle to undercount processes and mistakenly blame the source code; write `"$LOGDIR"/"$$".txt` and wait for a while before confirming "correct process".
- [The `fs.watch` event is not evidence that the file has changed](fs-watch-events-are-not-evidence-of-a-diff.md) — macOS replays the events of writes immediately before `watch()` was installed, so "path named by the event and present on both sides ⇒ Changed" generates spurious messages (a redundant re-import of `pom.xml` each time it boots); Use the event just to schedule the flush, and decide the type of change by diff `(mtimeMs, size, ino)` with the previous settle snapshot.
- [FSEvents startup window swallows the first write](fsevents-startup-window-swallows-the-first-write.md) — on macOS, libuv starts the FSEvents stream after `fs.watch()` returns, so writes that fall into that window never arrive; Symptoms are silence and then timeout at the first wait of **whichever shift records first** (measured 2/4 parallel runs in red, running separately, 12/12 green); fix with a bait file that the watcher ignores structurally, only firing when `lastChangeAt` is `undefined`.
- [Two guards for the same mutant survival time](two-guards-for-one-deadline-hide-a-live-mutant.md) — mutants that delete the correct falsifier of `INV-READY-3` still have a green 6/6 because the deadline is tightened in two places (the `settleBy` and `timeoutMs` loops sandwiched in `#probeOnce`); Living mutants is by no means equivalent — finding an input that disables the remaining guard layer, here an injectable probe deliberately omitted `timeoutMs`, and accepting `cancelled` due to suspension is valid red evidence for the "must settle within deadline" invariant.
- [Process-boundary Level 3 oracle for a build feature](process-boundary-oracle-for-build-features.md) — checker rejection for an in-process mock on a real spawn/exit boundary is the maker's own oracle to write (build-kind), not test-designer's; materialize scripted child fixtures to a tmpdir at test run time, and red-prove the oracle by temporarily breaking the exact source behavior before trusting green.
- [On Unix sockets, "N processes with bind" does not automatically report EADDRINUSE](unix-socket-bind-race-needs-an-o-excl-lock.md) — probe `connect()` before binding is not enough: each launcher comes after `unlink` the previous one's socket file and then binds the new file, so 5 simultaneous launches produce 5 daemons without ever reporting `EADDRINUSE` (measured by mutant M4); must wrap `probe → unlink stale → listen` in a lock `openSync(path,"wx")` that is kept for the life of the daemon and carries the pid.
- [Isolate each field of the triple `(mtimeMs, size, ino)`](isolating-one-field-of-a-stat-stamp.md) — ca temp-write-then-rename changes inode in all cases so it can never pin the mtime or size side; `writeFileSync` onto an existing path keeps the inode intact (`O_TRUNC`, not `unlink`), so writing in place with the same number of bytes isolates mtime while writing elsewhere the number of bytes with `utimesSync` returns the old datum isolating size — provided the datum was frozen in the past **before** `start()` and the write/`utimesSync` pair is in the same sync block.- [The "unobservable" inner layer is often exposed at the injectable port](inner-layer-shows-up-at-the-injectable-seam.md) — checker ignores mutant RM8 because it assumes that the inner timeout clamp layer is not measurable, but the corrupted quantity is **given** via the `ProbeOptions.timeoutMs` parameter of the public `probe` port: inject probe, record received budget, claim it is within `[1, caller's term]` is to kill immediately; Includes a tip to replace unlimited `await` with `Promise.race` with watchdog so that the mutant dies with a red line with its name instead of suspending and canceling all subsequent shifts.
- [Budgeting at the test layer only stops half of the crash](bounding-a-hang-at-the-test-layer-is-not-enough.md) — wrap `shutdown()` in `Promise.race` to make all named red shifts but the process still doesn't exit (measured: SIGKILL at 120 s), because what keeps the event loop is a listening `net.Server`, not a promise, so the deadline must be within `src`; with three consequences: the forcing path will turn mutant into equivalent if ca does not claim a budget that is completely below it, `# canceled != 0` with `# fail == 0` means there is a wait loop built on the timer that `unref()` exhausted the event loop, and `listen()` on the path surpasses `sun_path` **successfully** and truncate the name without reporting an error.
- [Returned-closures-need-a-case-that-calls-them.md) — the deregister function returned by `onNotification()` is emptied by the mutant `return () => {};` but 9/9 cases are still green, because the only evidence for it is `tsc` saying the type match; with two tips: the negative shift must have a positive anchor before it (`calls === 1` when the registration is valid) otherwise it is empty, and the handler removes itself mid-dispatch as `splice` translates the index so the sibling is silently ignored.
- [Invariant about stdout only measured in the real process](stdout-invariant-needs-the-real-process.md) — oracle injects a `Writable` into the `stdout` option that never sees `console.log`, because that function writes to the process's `process.stdout`; measured by mutant inserting exactly one log line: 6 shifts in the green process, only the shift running the module as a real child process and reading bytes on the real stdout pipe reports red — any "nothing but
- [Gating-a-real-seam-instead-of-faking-it.md) — "gating-a-real-seam-instead-of-faking-it.md" — the "kill component A, let B take its place on the same socket" switch creates a race: as soon as A dies the path is empty and the component under test itself binds it, so the green shift for different reasons each time it runs; Fix it by having the `launch` port still call the real function but `await` a promise held by the test, and always wait for the observable state of the component (`stats().connected` to turn false) rather than waiting for the prey to die — with a trick of attaching a `pid` to the answer to prove another real process has replied.
- [Recorder on `process.stdout.write` captures the reporting channel of `node --test`](stdout-recorder-must-ignore-the-runner-channel.md) — `node --test` runs each spec file in the child process and reports through `process.stdout` itself using the V8 serializer, so the naive recorder fakes the binary `test:start` frame; It turns out that `console.log` and all log lines of the source code always come as `string` and the runner channel always comes as `Buffer`, so just write the string chunk, with the assertion `NODE_TEST_CONTEXT === "child-v8"` and a positive anchor so that `recorded() === ""` is not empty.
- [Pinning "peer dies in the middle of a message" with a single socket write](pinning-a-mid-message-death-deterministically.md) — this scenario only works if the dead end was definitely in the buffer when the peer was SIGKILL, and a `setTimeout` does not prove that; Sending the truncated fragment in **the same `socket.write()`** as the previous response turns "the response went to stdout" into mechanical evidence — thus the double mutation (omitting `flush` + using the same framer) is revealed to completely disappear a response.- [UTF-8 character cutoff must be justified in bytes](proving-a-chunk-boundary-inside-a-utf8-character.md) — fixture *contains* a multibyte character that cannot distinguish `StringDecoder` from `chunk.toString()` (mutant `D1` lives twice because every boundary falls at the beginning of the character); Assert `(buffer[cut] & 0xc0) === 0x80`, prove the incoming chunk in two reads with an anchor message that goes along with the chunk plus the buffer, compare the full text rather than "parse into JSON" — and tell the oracle's own framer to use `StringDecoder`.
- [Generate a real `error` event on the socket](triggering-and-observing-a-real-socket-error.md) — destroy the peer socket **then** immediately write a large message for `EPIPE` 5/5 times, reverse order with small message for 0/5, `resetAndDestroy()` throws `ERR_INVALID_HANDLE_TYPE` on the Unix socket; and completely removing the `error` listener **doesn't** kill the process because `probeDaemon` leaves a settled `once("error")` swallowing the first error, so ca must have a positive anchor on stderr.
- [`LspClient` not routing notifications](lsp-client-not-dinh-tuyen-notification.md) — `onRequest` only runs when the message carries an `id`, so the LSP notification (publishDiagnostics, language/status) is left silent and there is no `notify()` for the sending direction; The push consuming component must declare the narrow port in its own file (`LspNotificationSource` / `LspNotificationSink`) — fix `src/lsp/lsp-client.ts` as scope bleed as ruled in `feat-file-sync-watcher` — and must note in `checkerNotes` that the actual dispatch section is missing.
- [`npm test -- <file>` appends to the glob, does not collapse it](npm-test-with-a-file-does-not-narrow-the-suite.md) — script `test` already contains three globs so the command in `verification` runs the ENTIRE suite; measured at `feat-tool-references`: 9 failures/100 while the oracle of the blue feature itself 12/12, all errors are in two maker files running in parallel — measured with `node --test <a file>`, then blamed by file with a loop before concluding.
- [This machine has no `timeout`, and a mutant pass without it is completely silent](no-coreutils-timeout-makes-a-mutant-pass-print-nothing.md) — both `timeout` and `gtimeout` are absent on macOS, `command not found` is swallowed by the script's own `grep`, so four mutants print four headers and no output lines — state indistinguishable from "all mutants survive"; Use node's `--test-timeout`, and always keep `# tests`/`# fail` in the grep pattern as proof that the command ran.
- [`timeout` not available on macOS: silent mutant build](gnu-timeout-missing-makes-a-mutant-pass-silently-empty.md) — `timeout 120 node --test ... | grep "^not ok"` prints exactly five mutant titles and not a single red line, which looks exactly like "all mutants survived", because darwin doesn't have GNU's `timeout` so `node` never ran; use `node --test --test-timeout=30000` (runner's own budget, cut off case still prints `not ok` with name) and make the script cry when a run fails to produce a `# tests N` line.
- [A leaked handle turns a red mutant into a hang](a-leaked-handle-turns-a-red-mutant-into-a-hang.md) — the mutant that removed the cleanup path also removed what kept the test process from exiting, so `node --test` got `SIGKILL` after timeout and did not print any `not ok` (measured: M6 took 600 s, no red line); The case verifying a cleanup invariant must have a safety net **not crossing the line being tested** (`t.after` directly closes each watcher, not just `pool.close()`), cleanup must be registered as soon as the resource is created rather than calling `close()` in the middle of the case, and the mutant build script must have a separate `timeout` + `killSignal` for each run.- [Every case awaits the resources, so no cases enter the half-built state](every-case-awaits-the-resource-so-half-built-never-runs.md) — six self-built mutants all died as expected and still had a real handle leak, because every case started with `await pool.acquire()`, so `"starting"` was only assigned and no cases observed; Count the number of **reads** of each value of the state type to find the blind area, and build a two-phase window using two `deferred` around the seam (`spawnReached` + `spawnGate`) rather than using `setTimeout`; with two tips: `grep` both the template string when the invariant says "switch in exactly one place", and emoji does NOT verify the surrogate pair (needs the astral letter `𝐀` to be in an identifier).
- [`EMFILE` from `fs.watch()` is the host's quota, and it heals itself](emfile-from-fs-watch-is-a-host-quota-that-heals.md) — The red baseline in `DiskFileSyncWatcher` looks exactly like a file descriptor leak, but a standalone Node process that did not import any of the project's modules also `EMFILE` on an EMPTY temp directory while watching a file and opening 300 fd `/dev/null` is still green — those three statements together rule out both the watcher error and normal fd exhaustion, the culprit being that macOS's directory watch quota is exhausted by multiple parallel test processes; it recovers on its own (measured again a day later: 202 green watches, baseline 124/124, no lines edited), so keep `readyForCheck: false` and measure again when the machine is idle instead of spending another round of trying to fix it.
- [The red `feat-001` case could be another maker's suite unit](integration-gate-tests-mirror-other-makers-red.md) — baseline-gate cases call `init.mjs` in child process, which gate runs `npm test` in its entirety, so they mirror maker's work-in-progress file in parallel (measure: 5 red + `# tests 320`, rerun `# tests 181`, 0 red, no correction); read the red shift NAME before reading the number, `# tests` jumping between two runs is a sign the source tree is being edited at the same time, and `npm test -- <file>` does NOT narrow the scope but just appends to an existing glob.