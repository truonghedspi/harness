# Checker memory — JDT MCP Server

Index of what the checker agent has learned across runs (`harness/docs/reference/agent-memory.md` documents the schema and why). One line per entry, always loaded — keep it
short; the reasoning lives in the linked file, read that only when the line looks relevant.

Write a new entry when a maker's claim looked right but wasn't (and how you actually caught it),
or a class of feature keeps needing the same scrutiny. Don't write one for a routine approve/reject
— that's the job working as intended, not a lesson.

Write entries and hooks **in English**, whatever language the rest of the project uses: memory is
addressed to whichever agent reads it next, not to this project's readers. Entries already written
in another language stay as they are — this governs what gets written from now on.

<!-- - [Title](slug.md) — one-line hook -->
- [Provisioner success path](provisioner-success-path.md) — Failure-path coverage can leave first-run installation untested.
- [Baseline gate oracle seam](baseline-gate-oracle-seam.md) — Helper-only tests do not prove that the startup gate calls the helper or propagates its failures.
- [Checksum rejection oracle](checksum-rejection-oracle.md) — Comparing copied archive contents does not prove checksum mismatch rejection.
- [Routing contract contradiction](routing-contract-contradiction.md) — A green routing oracle can encode one side of a multi-module contract contradiction.
- [Accepted risk validated by nothing](accepted-risk-validated-by-nothing.md) — "Validated only against the current fixture tree" can mean validated by no condition at all.
- [Sibling pre-authored oracle goes unrun](sibling-preauthored-oracle-goes-unrun.md) — When a build feature creates a module for the first time, run every pre-authored oracle that targets it; the feature's own verification command never does, and a red there is often an oracle defect, not an implementation defect.
- [Redundant fallback masks a mutant](redundant-fallback-masks-a-mutant.md) — Deleting SIGTERM proves nothing while the SIGKILL escalation still reaps the child; count the mechanisms in the function before designing the mutant.
- [Superlative in invariant needs two candidates](superlative-in-invariant-needs-two-candidates.md) — A word like outermost/nearest is verified by nothing unless a fixture offers two candidates on that axis; the qualifier clause after it (`that declares X`) is a third axis, and closing one axis per round trickles gaps forever.
- [Flake fixed inside the oracle hides a production defect](flake-fixed-inside-the-oracle-hides-a-production-defect.md) — A test-side workaround (probe file, warm-up loop) with no production counterpart means the flake was an implementation gap, not a test artifact; check `docs/assumptions.md` for the row the measurement already falsifies. Same entry: when the implementation decides on a tuple like `(mtime, size, ino)`, demand one case per field in a single round — closing one field per round trickles gaps forever.
- [Maker-authored mutants cover only branches he wrote](maker-authored-mutants-cover-only-branches-he-wrote.md) — On a build feature where the maker writes his own oracle, every mutant he reports can reproduce and the oracle still be blind; build yours from three axes he cannot see — protocol constants the test re-imports from the module under test, the mechanism his own rationale calls load-bearing, and the single fixture shape every case shares.
- [A test timeout does not cover the after hook](test-timeout-does-not-cover-the-after-hook.md) — `{ timeout: N }` bounds the test body only; an unbounded `await` in `t.after` (server.close, child.kill) hangs forever with no failure line, so aim one mutant at the cleanup path and watch the wall clock.
- [Green suite that never terminates](green-suite-that-never-terminates.md) — Bounding every `await` in cleanup catches shutdown that never settles, not shutdown that settles early and leaks a listening handle; flip the `if (!server.listening)` early-out and watch for `ok 1..N` followed by silence, and always run mutants of this class through a process-group-killing timeout wrapper.
- [Surviving mutant claimed equivalent](surviving-mutant-claimed-equivalent.md) — A mutant that survives on defensive code may truly be equivalent; accept it only after you rebuild it, read the synchronous window for a missing `await`, and run your own probe — then demand the comment be demoted from "this guarantees INV-X" to unproven redundancy.
- [Returned teardown function is an untested API surface](returned-teardown-function-is-an-untested-api-surface.md) — A method returning `() => void` (unsubscribe/detach/close) is a second API surface no case touches; `return () => {};` survives, and it hides a paired mutant because the only case that discriminates the dispatch-loop snapshot must call that returned function.
- [The positive anchor you request must also be measured by mutant](anchor-you-demanded-must-itself-be-mutated.md) — Last time you asked for an extra meaningless anti-green statement, next time make the exact same sentence; Reading it is testing presence, not effect. Debt exactly one mutant breaks the mechanism that the anchor asserts, then claims the CORRECT failure at the anchor line with its correct message.
- [The oracle maker's self-stated principle usually only applies two-thirds](mot-nguyen-tac-ap-hai-phan-ba.md) — When the maker explains why a class of shifts should be more expensive, count the number of protected mechanism call locations and compare it with the list of shifts running at the expensive level; The difference is the mutant to be built. Same entry: when a comment asserts immutability, count the number of mechanisms that simultaneously execute that statement — two mechanisms in two different functions support each other, causing every single mutant to survive, only a mutant that destroys the entire cluster will reveal the blind oracle.
- [Test case comment asserts fixture contains a boundary case — let's recalculate the arithmetic](fixture-comment-claims-a-boundary-it-does-not-contain.md) — "a non-ASCII character spanning a chunk boundary" turns out to be bogus: a 42-byte prefix plus a step of 30,000 bytes causes all boundaries to fall at the beginning of the character, so the mutant leaves `StringDecoder` alive. Same entry: before concluding that a mutant is equivalent, measure the extent of the branch with a file-recording probe; and consider how to build a new case before asking (`resetAndDestroy()` throws an error on Unix sockets, `EPIPE` is the way to go).
- [Mutant death proves the branch is useful, does not prove the reason in the comment](mutant-chet-khong-chung-minh-ly-do-trong-chu-thich.md) — A red shift because the timer expired waiting for a log line and a red shift because the process collapsed give the same color; Read the error message, not the color. Any helper that wraps `connect()` into a promise with `once("connect")` + `once("error")` leaves a listener settled on the returned socket, turning "immediate crash" into "silent swallow" in exactly one event — counting `listenerCount("error")` before trusting Node's default semantics.
- [Sandbox mutant located in the repo was detected by the test runner](sandbox-mutant-bi-runner-phat-hien.md) — `node --test` does not have the parameter to recursively scan the whole tree, so the mutant directories in `harness/trace/scratch/` are run together and create a fake red suite (1722 cases, 99 red, including the case under approval); Count cases and find duplicate case names before trusting, and clear the sandbox before every run of the entire suite.
- [Teardown runs before the startup can register](teardown-running-before-startup-kip-dang-ky.md) — All evict mutants run on fixtures that have already started, so no one touches the time axis: build probe with slow spawn seam, call acquire() without await then close(), count attached with detached (here 1 and 0, process does not exit). Same entry: a coordinate hidden in the template string of the error message is the second conversion boundary — grep the constant name instead of the "just two functions" comment.
- [Mutant corrupted syntax for an empty red color](mutant-hong-cu-phap-cho-mot-mau-do-rong-nghia.md) — Under `--experimental-strip-types`, a mutant using type syntax (`[] as typeof this.#field`) caused the spec file to fail to load; The TAP saying `not ok <file path>` looks exactly like the mutant that was killed. Compare `# tests` with m0 before trusting, and write mutants in plain JavaScript (`.slice(0, 0)`) rather than type annotations.- ["CODE-IDENTICAL" filters slash lines as puncture filters](code-identical-loc-dong-gach-cheo-khong-du.md) — When the planner grants limited editing rights to a block of comments, makes or proves "code unchanged" by filtering out the `//` line and then diff; That filter is blind to block comments and line comments. Ask "compared to which" first, then find the previous's pristine version in the session's shared scratchpad and run the full `diff -u` — `git diff` is useless when the previous feature hasn't been committed yet.
- ["Same relative path" is not "same cache key"](cung-duong-dan-tuong-doi-khong-phai-cung-khoa-cache.md) — List each component of the real key in the code and then ask the fixture whether to keep any components EQUAL between the two sides; If the composition is inherently different, the mutant that deletes it from the key will survive, although the phrase describing the scenario sounds a lot like a quarantine case. Same entry: before calling mutant is equivalent, run the component's existing oracle unit on the mutant itself.
- [One side of a union has been regularized before](a-ve-cua-phep-hop-da-can-be-standardized-tu-truoc.md) — When the fix wraps a normalization function on MULTIPLE sides of a union, do not completely revert: split into the correct number of halves, each half a mutant. Trace back to the birthplace of the value of each clause; Whichever side has passed through that function on the WRITE path, the call on the READ path is idempotent and no mutant can kill it — the comment stating "both right sides" is then only half correct (FOLLOW-UP, not REJECT).
- [Narrow ports accumulate unowned wiring debt](narrow-ports-accumulate-unowned-wiring-debt.md) — A locally declared port instead of scope bleed is the right boundary and each approval is correct, yet nobody owns the wiring; count the dangling ports and check whether the far-side component is already `done` before approving in silence.