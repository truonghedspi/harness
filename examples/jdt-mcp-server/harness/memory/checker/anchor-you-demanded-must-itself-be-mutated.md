# The positive anchor required by the checker itself must also be measured by the mutant

**When to apply:** Last turn you REJECT and ask the maker to add an anti-green-empty statement
("confirm handler WAS called before deletion", "confirm file existed before deletion", "confirm
cache has data before invalidate"). The next time the maker adds that exact sentence. Meet at
`feat-lsp-notifications` turn 2 (2026-08-23).

## Trap

You read that `assert.equal(calls, 1, "handler has never been connected")` is in the right place, in the right word, and
approval. But you just tested the anchor's **presence**, not its **effects**. Anchor
is an assertion like any other: it can also be written so that it is never red (compare
with the value generated by the code itself, reading the wrong variable, placing it after the call makes it always correct). Accept it because
it exists because of the exact type of error that you just forced the maker to fix in the previous turn.

## How to measure

Build a mutant that **breaks the exact mechanism that the anchor asserts**, not the mechanism that the test case claims
judgment. Here case 22 judges the road *unsubscribes*; The anchor confirms that the *dispatch* line is running. So
mutant must do dispatch without calling any handlers (`[...handlers]` → `[...handlers].slice(0, 0)`), then
assert that case fails **at the anchor line** with the correct anchor message — not fail at the assertion
In the end, it's not still green.

Three results, three ways to read:
- fail at anchor, correct message ⇒ anchor has real effect, approved.
- fails but in another assertion ⇒ redundant anchor, case can still be distinguished by something else; state clearly in the notes.
- still green ⇒ anchor is decorative, empty green test case means the same as before you REJECT.

## Rule of thumb

Each turn before you claim an additional claim, the next turn you owe exactly one mutant targeting the claim
that decision. One-time cost of running a suite; Without it, the maker–checker loop just goes from "code not working yet."
proven" to "checker's claim has not been proven". Apply at the same time as rebuilding the mutant
that maker reports — don't trust the numbers, run again.