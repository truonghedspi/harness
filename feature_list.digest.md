# Feature digest — generated, do not edit

One line per feature so an agent can see the whole shape cheaply. **`feature_list.json` remains
the source of truth** — but do not open it to read one entry. `node tools/feature.mjs <id>`
prints the full entry (behavior, verification, falsifier, evidence, checkerNotes) without loading
the whole file, and `--deps <id>` says whether it is eligible yet.

Regenerated by `init.sh`. 19 features — done: 6, blocked: 3, passing: 2, active: 1, not-started: 7

- `feat-windows-native-loop` **done** (1/3) — Autonomous loop runs natively on Windows
- `feat-dogfood-baseline` **blocked** — Canonical harness demo remains green
- `feat-dogfood-structure` **blocked** — Repository satisfies its own 13-lesson contract ← feat-dogfood-baseline
- `feat-dogfood-runtime` **blocked** — Generated runtime agents and verifier agree ← feat-dogfood-structure
- `feat-shared-memory-promote` **done** (1/3) — Mechanical promotion into memory/shared/ (v1)
- `feat-shared-memory-gate` **done** (1/3) — verify-harness gate for memory/shared/ ← feat-shared-memory-promote
- `feat-shared-memory-resources` **done** (1/3) — Every agent reads memory/shared/ read-only
- `feat-shared-memory-v1` **done** (1/3) — memory/shared/ v1 works end-to-end ← feat-shared-memory-promote,feat-shared-memory-gate,feat-shared-memory-resources
- `feat-readme-standardize` **done** (1/3) — README.md standardized for new users
- `feat-prompt-centralize` **passing** — All prompts are centrally managed in prompts/
- `feat-claude-code-acp` **passing** — Harness uses Claude Code ACP with claude-opus-4-6 ← feat-prompt-centralize
- `feat-prove-prompt-acp` **active** — Prove: prompt centralization and Claude Code ACP work end-to-end ← feat-prompt-centralize,feat-claude-code-acp
- `feat-toolchain-schema` **not-started** **ELIGIBLE** — test-toolchain.json schema and validator
- `feat-toolchain-resolve` **not-started** — resolve-test-command.mjs resolves scope to command ← feat-toolchain-schema
- `feat-toolchain-init` **not-started** — init.mjs reads test-toolchain.json for baseline ← feat-toolchain-schema
- `feat-toolchain-feature-scope` **not-started** — feature_list.json supports testScope field ← feat-toolchain-schema
- `feat-toolchain-survey` **not-started** — survey-project.mjs detects test runners and suggests toolchain ← feat-toolchain-schema
- `feat-toolchain-quality-strategy` **not-started** — check-quality-strategy.mjs validates scope against toolchain ← feat-toolchain-schema
- `feat-toolchain-e2e` **not-started** — Prove: pluggable test toolchain works end-to-end ← feat-toolchain-schema,feat-toolchain-resolve,feat-toolchain-init,feat-toolchain-feature-scope,feat-toolchain-survey,feat-toolchain-quality-strategy
