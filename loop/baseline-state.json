{
  "schema": "baseline-state/1",
  "status": "red",
  "evidenceDigest": "71aa9c74bd2cb20f",
  "exitCode": null,
  "checkedAt": "2026-09-03T08:25:36.215Z",
  "tail": "  OK  a tests/k8s journey routes to test-agent's integration mode, not maker\n  OK  one source, two files: Claude Code and kiro see the identical read-only cluster server\n  OK  no chart, no cluster layer — auto-detect does not push k8s onto projects that do not deploy that way\n  OK  a repo that ships a chart and verifies nothing against a cluster is flagged, not assumed fine\n  OK  an MCP server present for some runtimes and not others is caught, naming which — same agent, two verdicts\n\n── [36] multi-service k8s: dependsOn order, health that is not 'Running', ranked diagnostics ──\n  OK  installs in dependsOn order — db, then cache, then the api that needs both\n  OK  a service with no health command is reported unverified, not assumed healthy because helm --wait returned\n  OK  the test command runs only once every service is up\n  OK  a service can opt into an environment-specific Helm values file without changing chart defaults\n  OK  init and app containers are logged independently, so one unreadable container cannot mask another\n  OK  the environment records redacted deployment/readiness/scenario telemetry even on failure\n  OK  on failure the report ranks the failing service first, then its dependencies\n  OK  a failed Helm install uninstalls every attempted release and removes cluster-scoped state before retry\n  OK  a second isolated run succeeds after cleanup rather than inheriting Helm ownership\n  OK  legacy Helm recovery stays red on an annotation mismatch and mutates nothing\n  OK  exact legacy Helm orphan is adopted, uninstalled, verified absent, and its labelled recovery namespace removed\nfatal: your current branch 'main' does not have any commits yet\n"
}
