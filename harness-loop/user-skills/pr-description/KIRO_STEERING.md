---
inclusion: always
---

# Harness global skill: pr-description

Before drafting or writing a pull request description or PR body — or when asked to write one — read
and apply `{{SKILL_PATH}}`.

Skip only for a one-line fix, dependency bump, or typo, where the skill's own fast path already
says a title plus a sentence or two is enough. This steering file is only the activation bridge; the
installed `SKILL.md` is authoritative for the template and its boundaries.
