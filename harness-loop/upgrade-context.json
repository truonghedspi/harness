{
  "schema": "harness-upgrade-context/1",
  "entries": [
    {
      "id": "HUC-2026-08-16-node-native-loop",
      "summary": "The autonomous loop and named-agent dispatcher moved from Bash into Node.js.",
      "why": "Targets on native Windows could run the baseline but still needed Git Bash or WSL for the autonomous loop.",
      "targetImpact": "Core target orchestration now runs on Windows, macOS and Linux through one implementation; shell and cmd files are compatibility wrappers only.",
      "paths": [
        "loop/run-loop.mjs",
        "loop/run-loop.sh",
        "loop/run-loop.cmd",
        "loop/dispatch.mjs",
        "loop/dispatch.sh",
        "loop/dispatch.cmd",
        "AGENTS.md",
        "prompts/orchestrator.md",
        "loop/maker-prompt.md",
        "loop/checker-prompt.md",
        "loop/goal.md"
      ],
      "mergeActions": [
        "Preserve project-specific goals, commands and permissions in customized files.",
        "Replace workflow instructions that invoke run-loop.sh or dispatch.sh with node loop/run-loop.mjs or node loop/dispatch.mjs.",
        "Regenerate every runtime from agents.manifest.json after prompt merges."
      ],
      "verification": [
        "node loop/run-loop.mjs --headless exits cleanly when all features are settled",
        "node tools/gen-agents.mjs --target . --runtime all --check",
        "node tools/verify-harness.mjs --target . --skip-baseline"
      ]
    },
    {
      "id": "HUC-2026-08-17-upgrade-context-contract",
      "summary": "Harness updates now carry canonical upgrade context into the onboarder's merge plan.",
      "why": "Changed and drifted filename lists forced the onboarder to rediscover why a harness behavior changed and how target customizations should be reconciled.",
      "targetImpact": "Upgrade plans now retain the relevant reason, compatibility impact, merge actions and verification; a plan cannot pass while that context is unread or unacknowledged.",
      "paths": [
        "skills/harness-upgrade/**"
      ],
      "mergeActions": [
        "Refresh the harness-owned upgrade capability before final plan validation.",
        "Rebuild an old plan if its source report contains context IDs the plan omitted.",
        "Record an explicit target disposition for every applicable context entry."
      ],
      "verification": [
        "node skills/harness-upgrade/scripts/check-upgrade-plan.mjs <plan> rejects missing or unacknowledged source context",
        "node tools/verify-harness.mjs --target . --skip-baseline"
      ]
    },
    {
      "id": "HUC-2026-08-17-kiro-global-skill-activation",
      "summary": "User-scope skill installation now includes an always-loaded Kiro steering bridge.",
      "why": "A globally copied skill remained passive: Kiro agents could complete a turn without seeing its activation rule.",
      "targetImpact": "Kiro loads a thin global trigger for each installed harness user skill while the full workflow remains in the versioned SKILL.md.",
      "paths": [
        "user-skills/*/KIRO_STEERING.md",
        "scripts/install-user-skill.mjs"
      ],
      "mergeActions": [
        "Install or refresh each user skill and its Kiro steering bridge outside the target repository.",
        "Preserve local EXTEND.md customization before using --force.",
        "If a Kiro custom agent disables default-resource inheritance, explicitly include the global steering resource."
      ],
      "verification": [
        "Confirm ~/.kiro/steering/harness-skill-<name>.md uses inclusion: always and points to the installed SKILL.md.",
        "Run bash harness-loop/scripts/demo.sh."
      ]
    },
    {
      "id": "HUC-2026-08-17-orchestrator-native-spawn",
      "summary": "The interactive orchestrator now uses native sub-agent spawn as its primary execution mode.",
      "why": "Routing an interactive orchestration session back through a CLI script discarded the runtime's native parent-child coordination and made result collection indirect.",
      "targetImpact": "The router still chooses exactly one node, but orchestrator spawns that named role in-session and waits for it; Node dispatch remains available for headless, CI and unsupported runtimes.",
      "paths": [
        "prompts/orchestrator.md",
        "AGENTS.md",
        "agents.manifest.json"
      ],
      "mergeActions": [
        "Merge the native-spawn-first orchestrator contract without weakening route.mjs ownership.",
        "Regenerate runtime agent files from agents.manifest.json.",
        "Keep loop/dispatch.mjs and loop/run-loop.mjs for unattended execution and fallback."
      ],
      "verification": [
        "The orchestrator prompt requires the exact router-named role, one active child and post-spawn state inspection.",
        "Run bash harness-loop/scripts/demo.sh."
      ]
    },
    {
      "id": "HUC-2026-08-17-contained-harness-home",
      "summary": "Fresh scaffolds place harness state, machinery and documentation under root/harness.",
      "why": "Scattering tools, prompts, loop state, skills and harness documentation across the product root made experimental files hard to identify and remove; ignoring them hid all change visibility.",
      "targetImpact": "Root retains only AGENTS.md and runtime-mandated discovery adapters. The harness directory stays visible to Git, installation.json identifies changes without a product commit, and ignored harness/env/local.json contains redacted machine-specific configuration.",
      "paths": [
        "harness/**",
        "AGENTS.md",
        ".kiro/**",
        ".claude/**",
        ".codex/**"
      ],
      "mergeActions": [
        "Create the contained harness home and move only classified harness-owned files; do not move product-owned docs or tools by directory name alone.",
        "Keep root runtime adapters generated from harness/agents.manifest.json.",
        "Rewrite harness-owned references in prompts and docs to their harness/ paths; a physical move without instruction migration is incomplete.",
        "Run the router relative to its contained home while resolving generated runtime agents at project root.",
        "Capture local Java/Maven and Kubernetes selectors under harness/env; never copy API-key values into a tracked or checksummed artifact.",
        "Retain the legacy layout until target-specific drift has been classified; this structural migration is a human-reviewed merge."
      ],
      "verification": [
        "node harness/cli.mjs status",
        "node harness/cli.mjs route --json",
        "bash harness-loop/scripts/demo.sh"
      ]
    },
    {
      "id": "HUC-2026-08-18-design-facilitator",
      "summary": "`designer` and `design-reviewer` are retired and merged into one `design-facilitator` agent; the auto-reject loop between them is replaced by a human-authored `loop/design-approval.json`.",
      "why": "designer/design-reviewer could reject and re-dispatch each other with no human in the loop, and a design that changed even slightly between rounds reset the one-bounded-retry escalation counter, so rejection could cycle indefinitely — every round a paid session, and neither agent had the business context to know when a design was actually done. Full incident: docs/reference/graph.md row 11.",
      "targetImpact": "route.mjs no longer dispatches design-reviewer or bounces a rejected design back to designer; any design revision without an approval matching its exact digest routes straight to a human, always. loop/design-review.json is no longer produced or read. A design edited after approval — even by one line — silently invalidates the approval (digest mismatch), which is intentional: there is no partial-credit 'still basically approved' state.",
      "paths": [
        "prompts/design-facilitator.md",
        "prompts/designer.md",
        "prompts/design-reviewer.md",
        "agents.manifest.json",
        "loop/route.mjs",
        "loop/design-approval.json",
        "loop/design-review.json",
        "docs/design/README.md",
        "docs/reference/design-engineering.md",
        "docs/reference/critique-technique-sources.md",
        "memory/design-facilitator/**",
        "AGENTS.md"
      ],
      "mergeActions": [
        "If prompts/designer.md or prompts/design-reviewer.md were customized here, port the customization into prompts/design-facilitator.md by hand — a diff cannot merge two files into one.",
        "Delete the now-orphaned prompts/designer.md and prompts/design-reviewer.md, and their .kiro/.claude/.codex generated files, once customizations are ported — the file-diff refresh does not remove files that no longer exist in the canonical templates, only agents.manifest.json's newAgents/retiredAgents comparison surfaces that they are gone.",
        "If a feature is mid-flight in the old cycle (a NEEDS DESIGN: marker already answered, or a rejected loop/design-review.json sitting unresolved), resolve it under the old contract first, or explicitly re-open it: have a human review the current docs/design/*.md and write loop/design-approval.json before continuing.",
        "Delete any lingering loop/design-review.json — it is not read by the new routing rules and its presence is misleading.",
        "Regenerate every runtime from agents.manifest.json after the prompt merge."
      ],
      "verification": [
        "node loop/route.mjs --rules shows design-facilitator, not designer/design-reviewer, in the design-layer rows",
        "an unapproved docs/design/*.md routes to human, never to an agent (node loop/route.mjs --json)",
        "node tools/gen-agents.mjs --target . --runtime all --check",
        "bash harness-loop/scripts/demo.sh"
      ]
    },
    {
      "id": "HUC-2026-08-19-kiro-orchestrator-subagent-scope",
      "summary": "On kiro, orchestrator's generated config now declares the built-in `subagent` tool and a computed `toolsSettings.subagent.availableAgents` naming every other agent — enforcing 'never spawn the orchestrator itself' by config instead of prompt text alone.",
      "why": "kiro-cli has a native in-session `subagent` tool (verified against kiro.dev's docs, not assumed) gated by `toolsSettings.subagent.availableAgents`/`.trustedAgents`, but this harness's manifest never declared either for orchestrator — so on kiro specifically, 'never spawn the orchestrator itself' (prompts/orchestrator.md, 'What you must not do') was purely prompt-only, the same unenforced-MUST-NOT gap verify-harness.mjs's `rules` gate already flags elsewhere.",
      "targetImpact": "kiro's orchestrator.json gains `tools: [..., \"subagent\"]` and `toolsSettings.subagent.availableAgents` listing every other manifest agent (computed by gen-agents.mjs at generation time, so it never needs hand-updating when an agent is added or renamed). Claude Code and Codex are unaffected — no verified native in-session subagent mechanism is documented for them in this repo, so nothing changed there.",
      "paths": [
        "agents.manifest.json",
        "tools/gen-agents.mjs",
        "docs/reference/runtimes.md"
      ],
      "mergeActions": [
        "Add `subagentAllExceptSelf: true` and `\"subagent\"` in `tools` to this target's own orchestrator manifest entry — do not hand-list agent names; gen-agents.mjs computes the roster.",
        "If this target's orchestrator entry was customized (a narrower `tools` list, a hand-written `toolsSettings`), merge rather than overwrite — `subagentAllExceptSelf` only adds a `subagent` sub-key, it does not touch `toolsSettings.write`.",
        "Regenerate kiro's config and confirm the roster excludes only \"orchestrator\"."
      ],
      "verification": [
        "node tools/gen-agents.mjs --target . --runtime kiro --check",
        "require('./.kiro/agents/orchestrator.json').toolsSettings.subagent.availableAgents excludes \"orchestrator\" and includes every other agent",
        "bash harness-loop/scripts/demo.sh"
      ]
    },
    {
      "id": "HUC-2026-08-20-memory-bootstrap",
      "summary": "`memory-consolidate.mjs` gains `--bootstrap`: it mines `feature_list.json` checkerNotes and `trace/trace.jsonl` blocked/REJECT events for a reason that recurred across >=2 features and isn't already written into `memory/**`, and lists it as a candidate. Also removed the empty, orphaned `memory/context-interviewer/` template dir left over from the interviewer-agent retirement (HI-052).",
      "why": "A project that never wrote a memory entry does not start from zero knowledge — the raw material for a lesson (a checkerNotes reason a maker/checker kept re-hitting, a blocked reason that recurred) was already sitting in state files the harness already writes. Requiring someone to notice the pattern by eye before memory got its first real entry meant the mechanism only ever got exercised on projects mature enough to have already built the habit by hand.",
      "targetImpact": "`tools/memory-consolidate.mjs --target . --bootstrap [--json]` is a new, additive CLI surface; the existing structural report (index budget, orphans, duplicates) is unchanged and still runs without `--bootstrap`. It never writes to `memory/**` itself — same report-not-rewrite division of labor the rest of the script already uses. `memory/context-interviewer/` (template only, never scaffolded into a real project) is gone; nothing reads it.",
      "paths": [
        "tools/memory-consolidate.mjs",
        "docs/reference/agent-memory.md",
        "harness-loop/scripts/demo.sh"
      ],
      "mergeActions": [
        "Copy the updated `tools/memory-consolidate.mjs` into the target as-is — it is additive (a new flag), so a local customization to the structural-report path is unaffected.",
        "No action needed for the removed `memory/context-interviewer/` dir — it was never scaffolded into an existing target in the first place."
      ],
      "verification": [
        "node tools/memory-consolidate.mjs --target . --bootstrap --json",
        "bash harness-loop/scripts/demo.sh"
      ]
    },
    {
      "id": "HUC-2026-08-20-end-of-run-reflection",
      "summary": "Every writing agent's prompt (maker, checker, feature-planner, test-designer, test-implementer, harness-setup, design-facilitator, orchestrator) now closes with an explicit, uniformly-worded Yes/No reflection gate before it stops, instead of a one-off sentence phrased differently per agent. `test-implementer` previously had no closing memory guidance at all — that gap is now closed too.",
      "why": "`docs/reference/agent-memory.md` already named the `stop` hook's moment as 'the natural place to also prompt' this question, but the hook itself can only run a shell command (it fired `trace.mjs`'s `session-end` event and nothing else) — it cannot force an honest self-assessment. The enforceable half of that intention was always the prompt text, and it existed per-agent as loose prose an agent could skim past inside a longer numbered list, phrased slightly differently every time, which made it easy to treat as one more instruction rather than a forced branch to actually answer.",
      "targetImpact": "Each agent prompt's memory-writing instruction is now a literal two-branch question ('did this run produce something the next run shouldn't have to rediscover? Yes -> write an entry. No -> nothing to write') instead of a single conditional sentence. The underlying trigger criteria (a non-obvious mistake, an approach that worked for a non-obvious reason, an ambiguity that was hard to call, etc.) are unchanged per agent — only the framing is uniform. No hook or tool behavior changed.",
      "paths": [
        "prompts/feature-planner.md",
        "prompts/test-designer.md",
        "prompts/test-implementer.md",
        "prompts/harness-setup.md",
        "prompts/design-facilitator.md",
        "prompts/orchestrator.md",
        "loop/maker-prompt.md",
        "loop/checker-prompt.md"
      ],
      "mergeActions": [
        "If a target customized the closing memory paragraph of any of these prompts, port the customization's specific trigger criteria into the new Yes/No framing by hand — a mechanical diff would overwrite the customization.",
        "No tool, hook, or schema changed; this is prompt text only."
      ],
      "verification": [
        "grep -l \"End-of-.*reflection\" prompts/*.md loop/*-prompt.md",
        "bash harness-loop/scripts/demo.sh"
      ]
    },
    {
      "id": "HUC-2026-08-20-constraints-terse-state-and-toolbox",
      "summary": "Two new MUST rules in docs/constraints.md: write progress.md/DECISIONS.md as short bullets not prose, and check the existing tools/*.mjs toolbox before writing a new script.",
      "why": "Observed both problems in practice: progress.md entries drifting into essay-length prose, and ad-hoc one-off scripts/inline checks duplicating tools that already exist.",
      "targetImpact": "docs/constraints.md gains two MUST rules, auto-loaded into every writing agent. No new gate, no new script — prompt-only fix.",
      "paths": ["docs/constraints.md"],
      "mergeActions": ["Copy the two new bullets into the target's docs/constraints.md as-is."],
      "verification": ["grep -c 'MUST write .progress.md' docs/constraints.md"]
    },
    {
      "id": "HUC-2026-08-20-project-root-name-heuristic",
      "summary": "6 files (harness-cli.mjs, check-coverage.mjs, environment.mjs, init.mjs, loop/route.mjs, loop/dispatch.mjs) no longer infer 'contained layout' from directory name alone — a flat project named literally 'harness' was misdetected as contained, resolving PROJECT_ROOT one level too high.",
      "why": "Found live running upgrade-harness.mjs against this skill's own repo (named harness, flat layout): check-coverage.mjs flipped 3 lessons to blockers because its manifest/agent checks started reading the parent of the repo instead of the repo itself.",
      "targetImpact": "The heuristic now also requires the parent directory to have its own AGENTS.md (the thin pointer a contained scaffold writes) before treating a harness-named directory as contained. Only affects targets whose own directory happens to be named 'harness'.",
      "paths": ["harness-cli.mjs", "check-coverage.mjs", "tools/environment.mjs", "init.mjs", "loop/route.mjs", "loop/dispatch.mjs"],
      "mergeActions": ["Copy the 6 refreshed files as-is; the fix is additive to an existing condition, not a behavior change for any target not named 'harness'."],
      "verification": ["node check-coverage.mjs --target . --json", "bash harness-loop/scripts/demo.sh"]
    },
    {
      "id": "HUC-2026-08-20-check-plan-invariant-scoping",
      "summary": "check-plan.mjs's invariant-citation-missing check now only applies to a feature whose context.touches/note names the design file that declared the invariant; falsifier-orphan and invariant-uncovered stay global.",
      "why": "HI-053: on a project with multiple, temporally separate designs, adding invariants to ONE design retroactively flagged every unrelated open feature in the plan the moment it was added.",
      "targetImpact": "A feature from an unrelated subsystem no longer needs a citation just because some other docs/design/*.md elsewhere declares invariants. A feature that does cite a made-up invariant id is still caught (that check stayed global).",
      "paths": ["skills/feature-planning/scripts/check-plan.mjs"],
      "mergeActions": ["Copy the updated check-plan.mjs as-is; verify with skills/feature-planning/evals/run-fixtures.mjs (all 3 fixtures must still PASS)."],
      "verification": ["node skills/feature-planning/evals/run-fixtures.mjs", "bash harness-loop/scripts/demo.sh"]
    },
    {
      "id": "HUC-2026-08-20-graph-doc-layout-neutral",
      "summary": "docs/reference/graph.md's intro no longer asserts contained harness/ layout unconditionally.",
      "why": "HI-054: flat layout is the default; a flat target's copy of graph.md described paths (harness/cli.mjs, harness/AGENTS.md) that don't exist for it.",
      "targetImpact": "The intro paragraph now names both layouts. No path in the rest of the table changed.",
      "paths": ["docs/reference/graph.md"],
      "mergeActions": ["Copy the updated intro paragraph; the rest of the file is unaffected."],
      "verification": ["grep -q 'flat layout' docs/reference/graph.md"]
    },
    {
      "id": "HUC-2026-08-21-approval-gate-notify",
      "summary": "approval-gate.mjs fires a best-effort OS notification the moment it writes a fresh approval request, so a headless run does not silently sit until timeout with nobody having seen it.",
      "why": "Observed live: a headless run-loop.mjs sat at an approval gate with nobody watching that terminal. A safe timeout (APPROVAL_TIMEOUT_MIN) prevents an infinite hang, but auto-rejecting on a timeout nobody saw still spends the judgement this gate exists to get.",
      "targetImpact": "macOS uses osascript, Windows uses the built-in WinRT toast APIs (no extra module), Linux uses notify-send. HARNESS_NOTIFY=0 opts out; HARNESS_NOTIFY_CMD overrides with any `cmd title message`. A missing/broken notifier never blocks or fails the gate — it degrades to prior behavior.",
      "paths": ["loop/approval-gate.mjs"],
      "mergeActions": ["Copy the updated approval-gate.mjs as-is; no schema or CLI flag changed, only an additive notify() call."],
      "verification": ["bash harness-loop/scripts/demo.sh"]
    },
    {
      "id": "HUC-2026-08-21-state-log-prose-gate",
      "summary": "New verify-harness.mjs gate state-log-prose: flags (warn) >=3 consecutive non-bulleted lines in progress.md/DECISIONS.md.",
      "why": "docs/constraints.md already requires terse bullets there; nothing mechanical checked it. A crude structural proxy (no list marker) catches the common case without claiming real verbosity judgment.",
      "targetImpact": "New warn-only finding id state-log-prose:<file>:<line>. No blocker, no schema change.",
      "paths": ["tools/verify-harness.mjs"],
      "mergeActions": ["Copy the updated verify-harness.mjs as-is."],
      "verification": ["bash harness-loop/scripts/demo.sh"]
    },
    {
      "id": "HUC-2026-08-21-route-blocked-exclusion",
      "summary": "loop/route.mjs: the test-implementer rule and maker's unwritten-set filter now exclude status=blocked prove-features.",
      "why": "HI-056, found live on examples/jdt-mcp-server: `open` deliberately includes blocked features for visibility, but a rule that DISPATCHES action must not act on one — a blocked feature re-matched every iteration (observed 19 times) instead of staying stopped for a human.",
      "targetImpact": "A prove feature with status=blocked no longer triggers test-implementer, and no longer blocks an unrelated build feature via the unwritten-test check.",
      "paths": ["loop/route.mjs"],
      "mergeActions": ["If a target hand-patched this locally (as examples/jdt-mcp-server had), the canonical version now matches — copy over the local patch without re-diffing it."],
      "verification": ["bash harness-loop/scripts/demo.sh"]
    },
    {
      "id": "HUC-2026-08-21-promote-respects-reject",
      "summary": "verify-harness.mjs's --promote no longer promotes a feature whose checkerNotes starts with REJECT, even if its old verification command still exits 0.",
      "why": "HI-057, found live on examples/jdt-mcp-server: checker-prompt.md sets status back to in-progress on REJECT (so the router keeps offering it to the maker), which --promote could not distinguish from 'in-progress, never checked'. feat-lsp-client was rejected for lacking a real process-boundary oracle and was silently promoted to done on the next --promote pass anyway — a core-invariant violation ('only the checker sets done').",
      "targetImpact": "promoteFeatures() adds one guard: skip if /^REJECT\\b/ matches checkerNotes' first line (same marker convention route.mjs already uses for NEEDS DESIGN:/NEEDS RE-PLAN:).",
      "paths": ["tools/verify-harness.mjs"],
      "mergeActions": ["Copy the updated verify-harness.mjs as-is.", "Audit feature_list.json for any feature with status done/passing and checkerNotes starting with REJECT — that is exactly the corruption this fix prevents going forward, but an existing target may already carry one; revert its status to in-progress by hand."],
      "verification": ["bash harness-loop/scripts/demo.sh"]
    },
    {
      "id": "HUC-2026-08-22-semantic-review-batch",
      "summary": "Maker checkpoints no longer dispatch checker; readyForCheck now means a complete feature-level behavior with green verification and complete evidence, and attempts counts rejected review cycles.",
      "why": "HI-058: run-loop.mjs unconditionally dispatched checker after every maker iteration and maker-prompt.md unconditionally set readyForCheck, so small partial advances paid for an independent review before there was a complete claim to falsify.",
      "targetImpact": "A maker may commit several bounded checkpoints on one active feature with readyForCheck=false. run-loop.mjs skips checker until a complete claim is ready. Checker increments attempts on REJECT and blocks at maxAttempts; partial maker checkpoints no longer consume that budget.",
      "paths": ["loop/run-loop.mjs", "loop/maker-prompt.md", "loop/checker-prompt.md", "loop/goal.md", "docs/constraints.md", "feature_list.json", "prompts/k8s-integration-tester.md", "docs/reference/graph.md", "docs/reference/workflow-diagram.md", "docs/reference/feature-decomposition.md", "docs/reference/loop-engineering.md"],
      "mergeActions": ["Refresh the listed skill-owned files.", "For existing feature rows, reinterpret attempts as prior rejected review cycles where history permits; do not mechanically retain counts known to represent maker checkpoints.", "Do not set readyForCheck on partial work; leave the active feature false until its complete behavior and verification are green."],
      "verification": ["bash harness-loop/scripts/demo.sh", "node harness-loop/scripts/check-upgrade-context.mjs --target ."],
      "humanDecision": {
        "questionId": "semantic-review-batch-policy",
        "question": "Should checker run only for a semantically complete feature-level claim, with attempts counting failed reviews rather than maker checkpoints?",
        "answer": "phê duyệt",
        "answeredBy": "repository owner via chat",
        "answeredAt": "2026-08-22",
        "scope": ["default scaffold maker-checker workflow"],
        "basis": [{"kind": "observation", "pointer": "examples/jdt-mcp-server/harness/trace/tool-events.jsonl: 104 sessions, including repeated premature/redundant dispatch"}, {"kind": "human", "pointer": "chat approval on 2026-08-22"}],
        "supersedes": null,
        "openFollowUps": []
      }
    },
    {
      "id": "HUC-2026-08-22-codex-hook-adapter-calibration",
      "summary": "PreToolUse write confinement now serializes allow/deny per runtime: Claude keeps explicit allow/deny, Codex emits neutral allow and reasoned deny; Codex dispatch calibrates both branches first.",
      "why": "HI-059: Codex CLI 0.149.0 reports `PreToolUse hook returned unsupported permissionDecision:allow`. The shared Claude/Codex serializer made every permitted tool call a failed hook even though path classification was correct.",
      "targetImpact": "Generated Codex hooks pass `--runtime codex`; tools/hook-calibrate.mjs records the installed version and proves neutral allow plus reasoned deny before every Codex dispatch. loop/dispatch.mjs rejects hook-schema warnings even when the runtime exits zero. Existing Codex targets must refresh the generated hook and the four runtime tools together.",
      "paths": ["tools/guard-write.mjs", "tools/hook-calibrate.mjs", "tools/codex-dispatch.mjs", "tools/gen-agents.mjs", "tools/verify-harness.mjs", "loop/dispatch.mjs", ".codex/hooks.json", "docs/reference/runtimes.md", "docs/reference/graph.md"],
      "mergeActions": ["Refresh guard-write.mjs, hook-calibrate.mjs, codex-dispatch.mjs, verify-harness.mjs and loop/dispatch.mjs as one compatibility set.", "Regenerate .codex/hooks.json so its guard command includes `--runtime codex`.", "Run `node tools/hook-calibrate.mjs --target . --runtime codex`; do not dispatch Codex if adapter is not pass."],
      "verification": ["bash harness-loop/scripts/demo.sh", "node tools/hook-calibrate.mjs --target . --runtime codex"],
      "humanDecision": {
        "questionId": "runtime-hook-adapter-calibration",
        "question": "Implement runtime-specific hook adapters and live allow/deny calibration as the next harness improvement?",
        "answer": "đồng ý thực hiện như đề xuất",
        "answeredBy": "repository owner via chat",
        "answeredAt": "2026-08-22",
        "scope": ["default scaffold runtime hooks", "Codex dispatch acceptance"],
        "basis": [{"kind": "observation", "pointer": "Codex CLI 0.149.0 error: unsupported permissionDecision:allow"}, {"kind": "source", "pointer": "harness-loop/scripts/guard-write.mjs shared serializer"}, {"kind": "human", "pointer": "chat approval on 2026-08-22"}],
        "supersedes": null,
        "openFollowUps": []
      }
    }
  ]
}
