{
  "$comment": "Single source of truth for scope (Lesson 8). Every feature carries behavior + verification + state + evidence. Only the checker/verification script flips status to passing/done (Lesson 9). readyForCheck means the complete behavior is green AND reviewPacket passes tools/review-contract.mjs; partial checkpoints leave it false. attempts/maxAttempts counts failed semantic reviews, never incomplete submissions. context is the handoff INTO implementation. reviewPacket is the handoff OUT: contractDigest, claimRefs, changedPaths, exact command runs, five adversarial checks, and residualUnknowns. `node tools/review-contract.mjs <id> --json` prints the digest and admission errors. Evidence remains a list of red/green runs. behavior and falsifier are one sentence each; see docs/reference/test-authoring.md.",
  "states": [
    "not-started",
    "active",
    "blocked",
    "in-progress",
    "passing",
    "done"
  ],
  "features": [
    {
      "id": "feat-001",
      "name": "Baseline green",
      "behavior": "Project installs deps, runs the baseline gate to green, and starts from a clean checkout",
      "verification": "node init.mjs",
      "falsifier": "a gate that exits 0 without running anything — or one that runs a failing step and swallows it, which the bash version did — the baseline must be able to go red",
      "dependencies": [],
      "status": "not-started",
      "readyForCheck": false,
      "evidence": "",
      "checkerNotes": "",
      "attempts": 0,
      "maxAttempts": 3
    },
    {
      "id": "feat-002",
      "name": "First real feature (REPLACE ME)",
      "behavior": "Replace with the first concrete behavior the agent should implement",
      "verification": "REPLACE with a runnable command that proves this behavior, e.g. npm test -- feature-x",
      "context": {
        "touches": [
          "src/path/to/the-file-this-creates.ext",
          "src/path/to/the-one-it-changes.ext"
        ],
        "note": "One line the implementer would otherwise have to rediscover: the seam, the gotcha, the existing thing to reuse. Add context.packet only when the planner materializes a digest-bound packet for a non-obvious seam."
      },
      "falsifier": "REPLACE with the wrong implementation this command catches, e.g. 'returns the input unchanged' or 'rounds half-up instead of half-even'",
      "kind": "build",
      "dependencies": [
        "feat-001"
      ],
      "status": "not-started",
      "readyForCheck": false,
      "evidence": "",
      "checkerNotes": "",
      "attempts": 0,
      "maxAttempts": 3
    },
    {
      "id": "feat-003",
      "name": "Cross-service integration verification for feat-002 (REPLACE ME)",
      "behavior": "A full-pipeline check that the feature works across the microservice boundary — contract holds between the services involved (Lesson 10)",
      "verification": "REPLACE with the microservice integration / contract command, e.g. npm run test:contract or docker compose up + a cross-service flow test",
      "falsifier": "REPLACE with the cross-boundary defect this catches, e.g. 'service A serialises the field as a string while B expects a number'",
      "kind": "prove",
      "dependencies": [
        "feat-002"
      ],
      "status": "not-started",
      "readyForCheck": false,
      "evidence": "",
      "checkerNotes": "",
      "attempts": 0,
      "maxAttempts": 3
    }
  ]
}
