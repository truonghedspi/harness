

# Run output, including trace/scratch/ — the sanctioned home for a checker's throwaway probe, so it
# cannot be mistaken for project code or survive as unmaintained proof (loop/checker-prompt.md).
trace/
